# SortBy

Field to sort by. Defaults to created_at (newest first).

## Example Usage

```typescript
import { SortBy } from "@orq-ai/node/models/operations";

let value: SortBy = "display_name";
```

## Values

```typescript
"created_at" | "updated_at" | "display_name"
```