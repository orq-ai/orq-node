/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalsRagasCorrectness } from "../../funcs/evalsRagasCorrectness.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EvalsRagasCorrectnessRequestBody$inboundSchema.optional(),
};

export const tool$evalsRagasCorrectness: ToolDefinition<typeof args> = {
  name: "evals-ragas-correctness",
  description: `Run Correctness Evaluator`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evalsRagasCorrectness(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
