/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { datasetsList } from "../../funcs/datasetsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListDatasetsRequest$inboundSchema.optional(),
};

export const tool$datasetsList: ToolDefinition<typeof args> = {
  name: "datasets-list",
  description: `List datasets

Retrieves a paginated list of datasets for the current workspace. Results can be paginated using cursor-based pagination.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await datasetsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
