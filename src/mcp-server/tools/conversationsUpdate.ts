/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { conversationsUpdate } from "../../funcs/conversationsUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  requestBody: operations.UpdateConversationRequestBody$inboundSchema,
  conversationId: z.string(),
};

export const tool$conversationsUpdate: ToolDefinition<typeof args> = {
  name: "conversations-update",
  description: `Update conversation

Modifies an existing conversation's properties. Only the fields provided in the request body will be updated; all other fields remain unchanged. Changes are applied immediately.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await conversationsUpdate(
      client,
      args.requestBody,
      args.conversationId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
