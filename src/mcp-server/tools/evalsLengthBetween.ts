/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalsLengthBetween } from "../../funcs/evalsLengthBetween.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EvalsLengthBetweenRequestBody$inboundSchema.optional(),
};

export const tool$evalsLengthBetween: ToolDefinition<typeof args> = {
  name: "evals-length-between",
  description: `Run Length Between Evaluator`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evalsLengthBetween(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
