/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { datasetsRetrieveDatapoint } from "../../funcs/datasetsRetrieveDatapoint.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RetrieveDatapointRequest$inboundSchema,
};

export const tool$datasetsRetrieveDatapoint: ToolDefinition<typeof args> = {
  name: "datasets-retrieve-datapoint",
  description: `Retrieve a datapoint

Retrieves a datapoint object`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await datasetsRetrieveDatapoint(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
