/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { knowledgeList } from "../../funcs/knowledgeList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListKnowledgeBasesRequest$inboundSchema.optional(),
};

export const tool$knowledgeList: ToolDefinition<typeof args> = {
  name: "knowledge-list",
  description: `List all knowledge bases

Returns a list of your knowledge bases. The knowledge bases are returned sorted by creation date, with the most recent knowledge bases appearing first`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await knowledgeList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
