/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { datasetsDelete } from "../../funcs/datasetsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteDatasetRequest$inboundSchema,
};

export const tool$datasetsDelete: ToolDefinition<typeof args> = {
  name: "datasets-delete",
  description: `Delete a dataset

Permanently deletes a dataset and all its datapoints. This action is irreversible.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await datasetsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
