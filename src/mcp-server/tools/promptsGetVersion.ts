/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { promptsGetVersion } from "../../funcs/promptsGetVersion.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetPromptVersionRequest$inboundSchema,
};

export const tool$promptsGetVersion: ToolDefinition<typeof args> = {
  name: "prompts_get-version",
  description: `Retrieve a prompt version

Retrieves a specific version of a prompt by its ID and version ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await promptsGetVersion(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
