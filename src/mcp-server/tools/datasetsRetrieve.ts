/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { datasetsRetrieve } from "../../funcs/datasetsRetrieve.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RetrieveDatasetRequest$inboundSchema,
};

export const tool$datasetsRetrieve: ToolDefinition<typeof args> = {
  name: "datasets-retrieve",
  description: `Retrieve a dataset

Retrieves a specific dataset by its unique identifier`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await datasetsRetrieve(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
