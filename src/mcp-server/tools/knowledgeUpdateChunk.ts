/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { knowledgeUpdateChunk } from "../../funcs/knowledgeUpdateChunk.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateChunkRequest$inboundSchema,
};

export const tool$knowledgeUpdateChunk: ToolDefinition<typeof args> = {
  name: "knowledge-update-chunk",
  description: `Update a chunk`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await knowledgeUpdateChunk(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
