/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { datasetsUpdateDatapoint } from "../../funcs/datasetsUpdateDatapoint.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateDatapointRequest$inboundSchema,
};

export const tool$datasetsUpdateDatapoint: ToolDefinition<typeof args> = {
  name: "datasets-update-datapoint",
  description: `Update a datapoint`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await datasetsUpdateDatapoint(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
