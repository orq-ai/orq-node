/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { deploymentsMetricsCreate } from "../../funcs/deploymentsMetricsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeploymentCreateMetricRequest$inboundSchema,
};

export const tool$deploymentsMetricsCreate: ToolDefinition<typeof args> = {
  name: "deployments-metrics_create",
  description: `Add metrics

Add metrics to a deployment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await deploymentsMetricsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
