/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalsRagasSummarization } from "../../funcs/evalsRagasSummarization.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EvalsRagasSummarizationRequestBody$inboundSchema
    .optional(),
};

export const tool$evalsRagasSummarization: ToolDefinition<typeof args> = {
  name: "evals-ragas-summarization",
  description: `Run Summarization Evaluator`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evalsRagasSummarization(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
