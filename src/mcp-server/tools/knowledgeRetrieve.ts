/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { knowledgeRetrieve } from "../../funcs/knowledgeRetrieve.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOneKnowledgeRequest$inboundSchema,
};

export const tool$knowledgeRetrieve: ToolDefinition<typeof args> = {
  name: "knowledge-retrieve",
  description: `Retrieves a knowledge base

Retrieve a knowledge base with the settings.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await knowledgeRetrieve(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
