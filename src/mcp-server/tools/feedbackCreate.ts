/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { feedbackCreate } from "../../funcs/feedbackCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateFeedbackRequestBody$inboundSchema,
};

export const tool$feedbackCreate: ToolDefinition<typeof args> = {
  name: "feedback_create",
  description: `Submit feedback

Submit feedback for the LLM transaction via the API`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await feedbackCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
