/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { memoryStoresUpdateMemory } from "../../funcs/memoryStoresUpdateMemory.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateMemoryRequest$inboundSchema,
};

export const tool$memoryStoresUpdateMemory: ToolDefinition<typeof args> = {
  name: "memory-stores-update-memory",
  description: `Update a specific memory

Updates the details of a specific memory.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await memoryStoresUpdateMemory(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
