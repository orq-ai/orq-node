/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { knowledgeDeleteChunk } from "../../funcs/knowledgeDeleteChunk.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteChunkRequest$inboundSchema,
};

export const tool$knowledgeDeleteChunk: ToolDefinition<typeof args> = {
  name: "knowledge-delete-chunk",
  description: `Delete a chunk`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await knowledgeDeleteChunk(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
