/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { conversationsRetrieve } from "../../funcs/conversationsRetrieve.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  conversationId: z.string(),
};

export const tool$conversationsRetrieve: ToolDefinition<typeof args> = {
  name: "conversations-retrieve",
  description: `Retrieve conversation

Retrieves detailed information about a specific conversation identified by its unique ID. Returns the complete conversation object including metadata and timestamps.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await conversationsRetrieve(
      client,
      args.conversationId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
