/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalsAgeAppropriate } from "../../funcs/evalsAgeAppropriate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EvalsAgeAppropriateRequestBody$inboundSchema.optional(),
};

export const tool$evalsAgeAppropriate: ToolDefinition<typeof args> = {
  name: "evals-age-appropriate",
  description: `Run Age Appropriate Evaluator`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evalsAgeAppropriate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
