/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalsContainsEmail } from "../../funcs/evalsContainsEmail.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EvalsContainsEmailRequestBody$inboundSchema.optional(),
};

export const tool$evalsContainsEmail: ToolDefinition<typeof args> = {
  name: "evals-contains-email",
  description: `Run Contains Email Evaluator`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evalsContainsEmail(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
