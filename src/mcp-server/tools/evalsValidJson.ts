/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalsValidJson } from "../../funcs/evalsValidJson.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EvalsValidJsonRequestBody$inboundSchema.optional(),
};

export const tool$evalsValidJson: ToolDefinition<typeof args> = {
  name: "evals-valid-json",
  description: `Run JSON Validation Evaluator`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evalsValidJson(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
