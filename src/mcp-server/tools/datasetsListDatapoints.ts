/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { datasetsListDatapoints } from "../../funcs/datasetsListDatapoints.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListDatasetDatapointsRequest$inboundSchema,
};

export const tool$datasetsListDatapoints: ToolDefinition<typeof args> = {
  name: "datasets-list-datapoints",
  description: `List datapoints

Retrieves a paginated list of datapoints from a specific dataset.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await datasetsListDatapoints(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
