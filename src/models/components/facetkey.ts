/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { FacetValue, FacetValue$inboundSchema } from "./facetvalue.js";

/**
 * FacetKey is one facetable key inside a group, with its observed volume and
 *
 * @remarks
 *  top values for the requested range. field is the name usable in filters /
 *  sort (e.g. "attribute.http.method" or "service_name"); it is empty when the
 *  key is not filterable (the scope group).
 */
export type FacetKey = {
  key?: string | undefined;
  field?: string | undefined;
  logCount?: string | undefined;
  distinctValueCount?: string | undefined;
  topValues?: Array<FacetValue> | undefined;
  filterable?: boolean | undefined;
};

/** @internal */
export const FacetKey$inboundSchema: z.ZodType<
  FacetKey,
  z.ZodTypeDef,
  unknown
> = z.object({
  key: z.string().optional(),
  field: z.string().optional(),
  log_count: z.string().optional(),
  distinct_value_count: z.string().optional(),
  top_values: z.array(FacetValue$inboundSchema).optional(),
  filterable: z.boolean().optional(),
}).transform((v) => {
  return remap$(v, {
    "log_count": "logCount",
    "distinct_value_count": "distinctValueCount",
    "top_values": "topValues",
  });
});

export function facetKeyFromJSON(
  jsonString: string,
): SafeParseResult<FacetKey, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FacetKey$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FacetKey' from JSON`,
  );
}
