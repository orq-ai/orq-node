/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Comparison applied as `value <comparator> threshold`.
 */
export const Comparator = {
  Gt: "gt",
  Gte: "gte",
  Lt: "lt",
  Lte: "lte",
  Eq: "eq",
} as const;
/**
 * Comparison applied as `value <comparator> threshold`.
 */
export type Comparator = ClosedEnum<typeof Comparator>;

/**
 * Look-back window aggregated on each evaluation tick.
 */
export const Window = {
  Fivem: "5m",
  Fifteenm: "15m",
  Thirtym: "30m",
  Oneh: "1h",
  Fourh: "4h",
  Oned: "1d",
} as const;
/**
 * Look-back window aggregated on each evaluation tick.
 */
export type Window = ClosedEnum<typeof Window>;

/**
 * How often the alert is evaluated. Plan limits may enforce a minimum.
 */
export const Interval = {
  Fivem: "5m",
  Fifteenm: "15m",
  Oneh: "1h",
  Oned: "1d",
} as const;
/**
 * How often the alert is evaluated. Plan limits may enforce a minimum.
 */
export type Interval = ClosedEnum<typeof Interval>;

/**
 * Ingestion delay subtracted from the evaluation window. Defaults to `30s`.
 */
export const Delay = {
  Thirtys: "30s",
  Sixtys: "60s",
  OneHundredAndTwentys: "120s",
  ThreeHundreds: "300s",
  SixHundreds: "600s",
} as const;
/**
 * Ingestion delay subtracted from the evaluation window. Defaults to `30s`.
 */
export type Delay = ClosedEnum<typeof Delay>;

export type AlertCondition = {
  /**
   * Comparison applied as `value <comparator> threshold`.
   */
  comparator: Comparator;
  /**
   * Critical-tier threshold. Breaching it opens (or escalates) a
   *
   * @remarks
   *  trigger at critical severity.
   */
  threshold: number;
  /**
   * Optional degraded-tier threshold. Values past it but short of
   *
   * @remarks
   *  `threshold` open a trigger at degraded severity. Must sit on the
   *  recovery side of `threshold` for the chosen comparator and is not
   *  supported with `eq`.
   */
  degradedThreshold?: number | undefined;
  /**
   * Optional custom resolve bound (hysteresis). An open trigger
   *
   * @remarks
   *  resolves only once the value recovers past this bound; defaults to
   *  the lowest configured tier. Must sit at or past that tier on the
   *  recovery side. Not supported with `eq`.
   */
  resolveThreshold?: number | undefined;
  /**
   * Look-back window aggregated on each evaluation tick.
   */
  window: Window;
  /**
   * How often the alert is evaluated. Plan limits may enforce a minimum.
   */
  interval: Interval;
  /**
   * Ingestion delay subtracted from the evaluation window. Defaults to `30s`.
   */
  delay?: Delay | undefined;
};

/** @internal */
export const Comparator$inboundSchema: z.ZodNativeEnum<typeof Comparator> = z
  .nativeEnum(Comparator);
/** @internal */
export const Comparator$outboundSchema: z.ZodNativeEnum<typeof Comparator> =
  Comparator$inboundSchema;

/** @internal */
export const Window$inboundSchema: z.ZodNativeEnum<typeof Window> = z
  .nativeEnum(Window);
/** @internal */
export const Window$outboundSchema: z.ZodNativeEnum<typeof Window> =
  Window$inboundSchema;

/** @internal */
export const Interval$inboundSchema: z.ZodNativeEnum<typeof Interval> = z
  .nativeEnum(Interval);
/** @internal */
export const Interval$outboundSchema: z.ZodNativeEnum<typeof Interval> =
  Interval$inboundSchema;

/** @internal */
export const Delay$inboundSchema: z.ZodNativeEnum<typeof Delay> = z.nativeEnum(
  Delay,
);
/** @internal */
export const Delay$outboundSchema: z.ZodNativeEnum<typeof Delay> =
  Delay$inboundSchema;

/** @internal */
export const AlertCondition$inboundSchema: z.ZodType<
  AlertCondition,
  z.ZodTypeDef,
  unknown
> = z.object({
  comparator: Comparator$inboundSchema,
  threshold: z.number(),
  degraded_threshold: z.number().optional(),
  resolve_threshold: z.number().optional(),
  window: Window$inboundSchema,
  interval: Interval$inboundSchema,
  delay: Delay$inboundSchema.optional(),
}).transform((v) => {
  return remap$(v, {
    "degraded_threshold": "degradedThreshold",
    "resolve_threshold": "resolveThreshold",
  });
});
/** @internal */
export type AlertCondition$Outbound = {
  comparator: string;
  threshold: number;
  degraded_threshold?: number | undefined;
  resolve_threshold?: number | undefined;
  window: string;
  interval: string;
  delay?: string | undefined;
};

/** @internal */
export const AlertCondition$outboundSchema: z.ZodType<
  AlertCondition$Outbound,
  z.ZodTypeDef,
  AlertCondition
> = z.object({
  comparator: Comparator$outboundSchema,
  threshold: z.number(),
  degradedThreshold: z.number().optional(),
  resolveThreshold: z.number().optional(),
  window: Window$outboundSchema,
  interval: Interval$outboundSchema,
  delay: Delay$outboundSchema.optional(),
}).transform((v) => {
  return remap$(v, {
    degradedThreshold: "degraded_threshold",
    resolveThreshold: "resolve_threshold",
  });
});

export function alertConditionToJSON(alertCondition: AlertCondition): string {
  return JSON.stringify(AlertCondition$outboundSchema.parse(alertCondition));
}
export function alertConditionFromJSON(
  jsonString: string,
): SafeParseResult<AlertCondition, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AlertCondition$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AlertCondition' from JSON`,
  );
}
