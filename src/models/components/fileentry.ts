/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Kind of entry
 */
export const FileEntryType = {
  File: "file",
  Dir: "dir",
  Symlink: "symlink",
  Other: "other",
} as const;
/**
 * Kind of entry
 */
export type FileEntryType = ClosedEnum<typeof FileEntryType>;

/**
 * The file content messages below mirror the tools-pod JSON contract one for
 *
 * @remarks
 *  one. `path` is always relative to the file system root; an absolute path or a
 *  `..` segment is refused rather than clamped.
 */
export type FileEntry = {
  /**
   * Path of the entry relative to the file system root
   */
  path: string;
  /**
   * Final path component
   */
  name: string;
  /**
   * Kind of entry
   */
  type: FileEntryType;
  /**
   * Size of the entry in bytes
   */
  sizeBytes: string;
  /**
   * Last modification time of the entry
   */
  modified: string;
};

/** @internal */
export const FileEntryType$inboundSchema: z.ZodNativeEnum<
  typeof FileEntryType
> = z.nativeEnum(FileEntryType);

/** @internal */
export const FileEntry$inboundSchema: z.ZodType<
  FileEntry,
  z.ZodTypeDef,
  unknown
> = z.object({
  path: z.string(),
  name: z.string(),
  type: FileEntryType$inboundSchema,
  size_bytes: z.string(),
  modified: z.string(),
}).transform((v) => {
  return remap$(v, {
    "size_bytes": "sizeBytes",
  });
});

export function fileEntryFromJSON(
  jsonString: string,
): SafeParseResult<FileEntry, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FileEntry$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FileEntry' from JSON`,
  );
}
