/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  SharingAllProjects,
  SharingAllProjects$inboundSchema,
  SharingAllProjects$Outbound,
  SharingAllProjects$outboundSchema,
} from "./sharingallprojects.js";
import {
  SharingSelectedProjects,
  SharingSelectedProjects$inboundSchema,
  SharingSelectedProjects$Outbound,
  SharingSelectedProjects$outboundSchema,
} from "./sharingselectedprojects.js";

/**
 * Sharing controls which projects in the workspace may use this entity and
 *
 * @remarks
 *  what they may do with it.
 */
export type Sharing = {
  /**
   * Visible to every project in the workspace.
   */
  allProjects?: SharingAllProjects | undefined;
  /**
   * Visible to the listed projects only.
   */
  selected?: SharingSelectedProjects | undefined;
  /**
   * Consuming projects may pin a specific version instead of tracking
   *
   * @remarks
   *  latest. Enforcement lives with the consuming reference.
   */
  allowVersionPin?: boolean | undefined;
  /**
   * Projects may duplicate this entity into a detached project-owned
   *
   * @remarks
   *  copy.
   */
  allowFork?: boolean | undefined;
  /**
   * New projects created after this sharing config is applied should
   *
   * @remarks
   *  receive access automatically. Enforcement lives with project
   *  creation and entity-specific adoption code.
   */
  autoGrantNewProjects?: boolean | undefined;
};

/** @internal */
export const Sharing$inboundSchema: z.ZodType<Sharing, z.ZodTypeDef, unknown> =
  z.object({
    all_projects: SharingAllProjects$inboundSchema.optional(),
    selected: SharingSelectedProjects$inboundSchema.optional(),
    allow_version_pin: z.boolean().optional(),
    allow_fork: z.boolean().optional(),
    auto_grant_new_projects: z.boolean().optional(),
  }).transform((v) => {
    return remap$(v, {
      "all_projects": "allProjects",
      "allow_version_pin": "allowVersionPin",
      "allow_fork": "allowFork",
      "auto_grant_new_projects": "autoGrantNewProjects",
    });
  });
/** @internal */
export type Sharing$Outbound = {
  all_projects?: SharingAllProjects$Outbound | undefined;
  selected?: SharingSelectedProjects$Outbound | undefined;
  allow_version_pin?: boolean | undefined;
  allow_fork?: boolean | undefined;
  auto_grant_new_projects?: boolean | undefined;
};

/** @internal */
export const Sharing$outboundSchema: z.ZodType<
  Sharing$Outbound,
  z.ZodTypeDef,
  Sharing
> = z.object({
  allProjects: SharingAllProjects$outboundSchema.optional(),
  selected: SharingSelectedProjects$outboundSchema.optional(),
  allowVersionPin: z.boolean().optional(),
  allowFork: z.boolean().optional(),
  autoGrantNewProjects: z.boolean().optional(),
}).transform((v) => {
  return remap$(v, {
    allProjects: "all_projects",
    allowVersionPin: "allow_version_pin",
    allowFork: "allow_fork",
    autoGrantNewProjects: "auto_grant_new_projects",
  });
});

export function sharingToJSON(sharing: Sharing): string {
  return JSON.stringify(Sharing$outboundSchema.parse(sharing));
}
export function sharingFromJSON(
  jsonString: string,
): SafeParseResult<Sharing, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Sharing$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Sharing' from JSON`,
  );
}
