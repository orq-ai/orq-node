/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { ClosedEnum } from "../../types/enums.js";

/**
 * Reasoning effort for supported models. Omit to use the provider default.
 */
export const ReasoningEffort = {
  None: "none",
  Minimal: "minimal",
  Low: "low",
  Medium: "medium",
  High: "high",
  Xhigh: "xhigh",
  Max: "max",
} as const;
/**
 * Reasoning effort for supported models. Omit to use the provider default.
 */
export type ReasoningEffort = ClosedEnum<typeof ReasoningEffort>;

/**
 * Lets the primary model consult a configured secondary model for advice.
 */
export type OrqAdvisorTool = {
  /**
   * Maximum secondary-model output tokens. 0 uses the provider default; the selected model may impose a lower maximum.
   */
  maxTokens?: number | undefined;
  /**
   * Maximum estimated conversation-transcript tokens sent to the secondary model. 0 includes the full transcript.
   */
  maxTranscriptTokens?: number | undefined;
  /**
   * Maximum invocations per request. 0 means unlimited.
   */
  maxUses?: number | undefined;
  /**
   * Secondary model in provider/model format.
   */
  model: string;
  /**
   * Reasoning effort for supported models. Omit to use the provider default.
   */
  reasoningEffort?: ReasoningEffort | undefined;
  /**
   * Sampling temperature. The selected model may impose a lower maximum.
   */
  temperature?: number | undefined;
  /**
   * Advisor tool discriminator.
   */
  type: "orq:advisor";
};

/** @internal */
export const ReasoningEffort$outboundSchema: z.ZodNativeEnum<
  typeof ReasoningEffort
> = z.nativeEnum(ReasoningEffort);

/** @internal */
export type OrqAdvisorTool$Outbound = {
  max_tokens?: number | undefined;
  max_transcript_tokens?: number | undefined;
  max_uses?: number | undefined;
  model: string;
  reasoning_effort?: string | undefined;
  temperature?: number | undefined;
  type: "orq:advisor";
};

/** @internal */
export const OrqAdvisorTool$outboundSchema: z.ZodType<
  OrqAdvisorTool$Outbound,
  z.ZodTypeDef,
  OrqAdvisorTool
> = z.object({
  maxTokens: z.number().int().optional(),
  maxTranscriptTokens: z.number().int().optional(),
  maxUses: z.number().int().optional(),
  model: z.string(),
  reasoningEffort: ReasoningEffort$outboundSchema.optional(),
  temperature: z.number().optional(),
  type: z.literal("orq:advisor"),
}).transform((v) => {
  return remap$(v, {
    maxTokens: "max_tokens",
    maxTranscriptTokens: "max_transcript_tokens",
    maxUses: "max_uses",
    reasoningEffort: "reasoning_effort",
  });
});

export function orqAdvisorToolToJSON(orqAdvisorTool: OrqAdvisorTool): string {
  return JSON.stringify(OrqAdvisorTool$outboundSchema.parse(orqAdvisorTool));
}
