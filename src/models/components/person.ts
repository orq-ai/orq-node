/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { PersonStatus, PersonStatus$inboundSchema } from "./personstatus.js";

export type Person = {
  /**
   * Unique workspace user identifier (ULID).
   */
  id: string;
  /**
   * Linked account ID.
   */
  accountId: string;
  /**
   * Email address.
   */
  email: string;
  /**
   * Full display name (firstname + lastname).
   */
  displayName: string;
  /**
   * Profile image URL.
   */
  avatarUrl?: string | undefined;
  /**
   * Permission roles assigned to the person.
   */
  roles: Array<string>;
  /**
   * Group IDs the person belongs to (non-support groups).
   */
  groups: Array<string>;
  status: PersonStatus;
  /**
   * Last activity timestamp.
   */
  lastActivity?: Date | undefined;
  /**
   * When the person was invited or added.
   */
  createdAt: Date;
  /**
   * Last modification timestamp.
   */
  updatedAt: Date;
};

/** @internal */
export const Person$inboundSchema: z.ZodType<Person, z.ZodTypeDef, unknown> = z
  .object({
    id: z.string(),
    account_id: z.string(),
    email: z.string(),
    display_name: z.string(),
    avatar_url: z.string().optional(),
    roles: z.array(z.string()),
    groups: z.array(z.string()),
    status: PersonStatus$inboundSchema,
    last_activity: z.string().datetime({ offset: true }).transform(v =>
      new Date(v)
    ).optional(),
    created_at: z.string().datetime({ offset: true }).transform(v =>
      new Date(v)
    ),
    updated_at: z.string().datetime({ offset: true }).transform(v =>
      new Date(v)
    ),
  }).transform((v) => {
    return remap$(v, {
      "account_id": "accountId",
      "display_name": "displayName",
      "avatar_url": "avatarUrl",
      "last_activity": "lastActivity",
      "created_at": "createdAt",
      "updated_at": "updatedAt",
    });
  });

export function personFromJSON(
  jsonString: string,
): SafeParseResult<Person, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Person$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Person' from JSON`,
  );
}
