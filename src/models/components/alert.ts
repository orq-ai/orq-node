/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  AlertCondition,
  AlertCondition$inboundSchema,
} from "./alertcondition.js";
import { AlertQuery, AlertQuery$inboundSchema } from "./alertquery.js";
import { AlertRun, AlertRun$inboundSchema } from "./alertrun.js";

/**
 * UI signal preset the alert was created from.
 */
export const Signal = {
  Cost: "cost",
  Latency: "latency",
  Errors: "errors",
  Evals: "evals",
  Custom: "custom",
} as const;
/**
 * UI signal preset the alert was created from.
 */
export type Signal = ClosedEnum<typeof Signal>;

/**
 * Current runtime status, maintained by the evaluation engine.
 */
export const AlertStatus = {
  Ok: "ok",
  Degraded: "degraded",
  Triggered: "triggered",
} as const;
/**
 * Current runtime status, maintained by the evaluation engine.
 */
export type AlertStatus = ClosedEnum<typeof AlertStatus>;

export type Alert = {
  /**
   * Unique alert identifier assigned by ORQ, for example `alert_01H...`.
   *
   * @remarks
   *  Use this value in retrieve, update, and delete requests.
   */
  alertId: string;
  /**
   * Workspace-unique display name.
   */
  displayName: string;
  /**
   * Short human-readable summary of what the alert watches.
   */
  description: string;
  /**
   * Project that owns the alert. Immutable after creation.
   */
  projectId: string;
  /**
   * UI signal preset the alert was created from.
   */
  signal: Signal;
  /**
   * Metric query evaluated on each tick.
   */
  query: AlertQuery;
  /**
   * Threshold condition applied to the query result.
   */
  condition: AlertCondition;
  /**
   * Notifiers that receive trigger-open and trigger-resolve
   *
   * @remarks
   *  notifications.
   */
  notifierIds: Array<string>;
  /**
   * Whether the alert is evaluated. Disabled alerts keep their trigger
   *
   * @remarks
   *  history but stop firing.
   */
  enabled: boolean;
  /**
   * Current runtime status, maintained by the evaluation engine.
   */
  status: AlertStatus;
  /**
   * Time when the alert last opened a trigger.
   */
  lastTriggeredAt?: Date | undefined;
  /**
   * Rolling window of the most recent evaluation ticks, oldest first.
   *
   * @remarks
   *  Maintained by the evaluation engine; read-only.
   */
  recentRuns?: Array<AlertRun> | undefined;
  /**
   * Time when the alert was created.
   */
  createdAt: Date;
  /**
   * Time when the alert was last updated.
   */
  updatedAt: Date;
  /**
   * User ID that created the alert.
   */
  createdById: string;
  /**
   * User ID that last updated the alert.
   */
  updatedById: string;
};

/** @internal */
export const Signal$inboundSchema: z.ZodNativeEnum<typeof Signal> = z
  .nativeEnum(Signal);

/** @internal */
export const AlertStatus$inboundSchema: z.ZodNativeEnum<typeof AlertStatus> = z
  .nativeEnum(AlertStatus);

/** @internal */
export const Alert$inboundSchema: z.ZodType<Alert, z.ZodTypeDef, unknown> = z
  .object({
    alert_id: z.string(),
    display_name: z.string(),
    description: z.string(),
    project_id: z.string(),
    signal: Signal$inboundSchema,
    query: AlertQuery$inboundSchema,
    condition: AlertCondition$inboundSchema,
    notifier_ids: z.array(z.string()),
    enabled: z.boolean(),
    status: AlertStatus$inboundSchema,
    last_triggered_at: z.string().datetime({ offset: true }).transform(v =>
      new Date(v)
    ).optional(),
    recent_runs: z.array(AlertRun$inboundSchema).optional(),
    created_at: z.string().datetime({ offset: true }).transform(v =>
      new Date(v)
    ),
    updated_at: z.string().datetime({ offset: true }).transform(v =>
      new Date(v)
    ),
    created_by_id: z.string(),
    updated_by_id: z.string(),
  }).transform((v) => {
    return remap$(v, {
      "alert_id": "alertId",
      "display_name": "displayName",
      "project_id": "projectId",
      "notifier_ids": "notifierIds",
      "last_triggered_at": "lastTriggeredAt",
      "recent_runs": "recentRuns",
      "created_at": "createdAt",
      "updated_at": "updatedAt",
      "created_by_id": "createdById",
      "updated_by_id": "updatedById",
    });
  });

export function alertFromJSON(
  jsonString: string,
): SafeParseResult<Alert, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Alert$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Alert' from JSON`,
  );
}
