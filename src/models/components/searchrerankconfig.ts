/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";

export type SearchRerankConfig = {
  /**
   * The name of the rerank model to use. Refer to the [model list](https://docs.orq.ai/docs/proxy#/rerank-models).
   */
  model: string;
  /**
   * The threshold value used to filter the rerank results, only documents with a relevance score greater than the threshold will be returned
   */
  threshold?: number | undefined;
  /**
   * The number of top results to return after reranking. If not provided, will default to the knowledge base configured `top_k`.
   */
  topK?: number | undefined;
};

/** @internal */
export type SearchRerankConfig$Outbound = {
  model: string;
  threshold: number;
  top_k: number;
};

/** @internal */
export const SearchRerankConfig$outboundSchema: z.ZodType<
  SearchRerankConfig$Outbound,
  z.ZodTypeDef,
  SearchRerankConfig
> = z.object({
  model: z.string(),
  threshold: z.number().default(0),
  topK: z.number().int().default(10),
}).transform((v) => {
  return remap$(v, {
    topK: "top_k",
  });
});

export function searchRerankConfigToJSON(
  searchRerankConfig: SearchRerankConfig,
): string {
  return JSON.stringify(
    SearchRerankConfig$outboundSchema.parse(searchRerankConfig),
  );
}
