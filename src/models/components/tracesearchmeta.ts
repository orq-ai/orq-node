/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type TraceSearchMeta = {
  requestId?: string | undefined;
  from?: Date | undefined;
  to?: Date | undefined;
  rowCount?: number | undefined;
};

/** @internal */
export const TraceSearchMeta$inboundSchema: z.ZodType<
  TraceSearchMeta,
  z.ZodTypeDef,
  unknown
> = z.object({
  request_id: z.string().optional(),
  from: z.string().datetime({ offset: true }).transform(v => new Date(v))
    .optional(),
  to: z.string().datetime({ offset: true }).transform(v => new Date(v))
    .optional(),
  row_count: z.number().int().optional(),
}).transform((v) => {
  return remap$(v, {
    "request_id": "requestId",
    "row_count": "rowCount",
  });
});

export function traceSearchMetaFromJSON(
  jsonString: string,
): SafeParseResult<TraceSearchMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TraceSearchMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TraceSearchMeta' from JSON`,
  );
}
