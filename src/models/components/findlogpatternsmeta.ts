/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type FindLogPatternsMeta = {
  requestId: string;
  from: Date;
  to: Date;
  /**
   * Exact number of records matching the request before bounded sampling.
   */
  totalCount: string;
  sampledCount: string;
  truncated: boolean;
  warnings: Array<string>;
};

/** @internal */
export const FindLogPatternsMeta$inboundSchema: z.ZodType<
  FindLogPatternsMeta,
  z.ZodTypeDef,
  unknown
> = z.object({
  request_id: z.string(),
  from: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  to: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  total_count: z.string(),
  sampled_count: z.string(),
  truncated: z.boolean(),
  warnings: z.array(z.string()),
}).transform((v) => {
  return remap$(v, {
    "request_id": "requestId",
    "total_count": "totalCount",
    "sampled_count": "sampledCount",
  });
});

export function findLogPatternsMetaFromJSON(
  jsonString: string,
): SafeParseResult<FindLogPatternsMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FindLogPatternsMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FindLogPatternsMeta' from JSON`,
  );
}
