/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The verdict. Shaped to match WorkflowRunMinifiedEvalSchema, the body the
 *
 * @remarks
 *  TypeScript route returned, so existing consumers read the same JSON.
 */
export type EvaluationResult = {
  /**
   * Discriminator, matching the legacy union: "string", "number", "boolean",
   *
   * @remarks
   *  "string_array", "rouge_n", "bert_score", "llm_evaluator", "http_eval".
   */
  type?: string | undefined;
  /**
   * The verdict. Dynamic by design — a boolean pass, a numeric score, a
   *
   * @remarks
   *  categorical label, a list of labels and the nested rouge_n object all
   *  arrive here.
   */
  value?: any | undefined;
  /**
   * Trace reference of the evaluator's own span. Optional so an absent
   *
   * @remarks
   *  reference is omitted rather than emitted as an empty string, matching the
   *  legacy body.
   */
  traceId?: string | undefined;
  spanId?: string | undefined;
  evaluatorId?: string | undefined;
  /**
   * How the run ended, as distinct from `passed`: "passed", "condition_failed"
   *
   * @remarks
   *  or "timed_out". A string, not an enum, because the engine owns the
   *  vocabulary.
   */
  status?: string | undefined;
  /**
   * The guardrail's decision when the evaluator has one, the grader's own
   *
   * @remarks
   *  judgement otherwise. Always present — the endpoint this replaces omitted it
   *  without a guardrail, so read `guardrail_config` to detect one, not this.
   */
  passed?: boolean | undefined;
  explanation?: string | undefined;
  /**
   * Set by the classifying graders (moderation, PII, secret detection), which
   *
   * @remarks
   *  report which categories tripped rather than a single verdict.
   */
  categories?: Array<string> | undefined;
  /**
   * Optional so an absent score is omitted rather than reported as 0.0, which
   *
   * @remarks
   *  a consumer would read as maximum uncertainty.
   */
  confidence?: number | undefined;
};

/** @internal */
export const EvaluationResult$inboundSchema: z.ZodType<
  EvaluationResult,
  z.ZodTypeDef,
  unknown
> = z.object({
  type: z.string().optional(),
  value: z.any().optional(),
  trace_id: z.string().optional(),
  span_id: z.string().optional(),
  evaluator_id: z.string().optional(),
  status: z.string().optional(),
  passed: z.boolean().optional(),
  explanation: z.string().optional(),
  categories: z.array(z.string()).optional(),
  confidence: z.number().optional(),
}).transform((v) => {
  return remap$(v, {
    "trace_id": "traceId",
    "span_id": "spanId",
    "evaluator_id": "evaluatorId",
  });
});

export function evaluationResultFromJSON(
  jsonString: string,
): SafeParseResult<EvaluationResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EvaluationResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EvaluationResult' from JSON`,
  );
}
