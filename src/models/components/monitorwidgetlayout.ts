/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type MonitorWidgetLayout = {
  /**
   * Column offset in grid units.
   */
  x: number;
  /**
   * Row offset in grid units.
   */
  y: number;
  /**
   * Width in grid units.
   */
  w: number;
  /**
   * Height in grid units.
   */
  h: number;
};

/** @internal */
export const MonitorWidgetLayout$inboundSchema: z.ZodType<
  MonitorWidgetLayout,
  z.ZodTypeDef,
  unknown
> = z.object({
  x: z.number().int(),
  y: z.number().int(),
  w: z.number().int(),
  h: z.number().int(),
});
/** @internal */
export type MonitorWidgetLayout$Outbound = {
  x: number;
  y: number;
  w: number;
  h: number;
};

/** @internal */
export const MonitorWidgetLayout$outboundSchema: z.ZodType<
  MonitorWidgetLayout$Outbound,
  z.ZodTypeDef,
  MonitorWidgetLayout
> = z.object({
  x: z.number().int(),
  y: z.number().int(),
  w: z.number().int(),
  h: z.number().int(),
});

export function monitorWidgetLayoutToJSON(
  monitorWidgetLayout: MonitorWidgetLayout,
): string {
  return JSON.stringify(
    MonitorWidgetLayout$outboundSchema.parse(monitorWidgetLayout),
  );
}
export function monitorWidgetLayoutFromJSON(
  jsonString: string,
): SafeParseResult<MonitorWidgetLayout, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MonitorWidgetLayout$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MonitorWidgetLayout' from JSON`,
  );
}
