/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import {
  collectExtraKeys as collectExtraKeys$,
  safeParse,
} from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A `response.reasoning_summary_part.done` server-sent event.
 */
export type ResponseReasoningSummaryPartDoneStreamEvent = {
  /**
   * ID of the output item this event refers to.
   */
  itemId: string;
  /**
   * Index of the output item in the response output array.
   */
  outputIndex: number;
  /**
   * The reasoning summary part.
   */
  part: { [k: string]: any };
  /**
   * Monotonically increasing sequence number for ordering events.
   */
  sequenceNumber: number;
  /**
   * Index of the reasoning summary part.
   */
  summaryIndex: number;
  /**
   * The event type. Discriminates the payload.
   */
  type: "response.reasoning_summary_part.done";
  additionalProperties?: { [k: string]: any } | undefined;
};

/** @internal */
export const ResponseReasoningSummaryPartDoneStreamEvent$inboundSchema:
  z.ZodType<
    ResponseReasoningSummaryPartDoneStreamEvent,
    z.ZodTypeDef,
    unknown
  > = collectExtraKeys$(
    z.object({
      item_id: z.string(),
      output_index: z.number().int(),
      part: z.record(z.any()),
      sequence_number: z.number().int(),
      summary_index: z.number().int(),
      type: z.literal("response.reasoning_summary_part.done"),
    }).catchall(z.any()),
    "additionalProperties",
    true,
  ).transform((v) => {
    return remap$(v, {
      "item_id": "itemId",
      "output_index": "outputIndex",
      "sequence_number": "sequenceNumber",
      "summary_index": "summaryIndex",
    });
  });

export function responseReasoningSummaryPartDoneStreamEventFromJSON(
  jsonString: string,
): SafeParseResult<
  ResponseReasoningSummaryPartDoneStreamEvent,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      ResponseReasoningSummaryPartDoneStreamEvent$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'ResponseReasoningSummaryPartDoneStreamEvent' from JSON`,
  );
}
