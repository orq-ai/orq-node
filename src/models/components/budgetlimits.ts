/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  BudgetPeriod,
  BudgetPeriod$inboundSchema,
  BudgetPeriod$outboundSchema,
} from "./budgetperiod.js";

/**
 * BudgetLimits is the per-period spend and token ceiling. At least one
 *
 * @remarks
 *  of `amount`, `token_limit`, or RateLimit.requests_per_minute MUST be
 *  set on a Budget; that invariant is enforced by the handler.
 */
export type BudgetLimits = {
  period?: BudgetPeriod | undefined;
  amount?: number | undefined;
  /**
   * Token ceiling. Carried as a double so it serializes as a JSON number
   *
   * @remarks
   *  (proto int64 would serialize as a quoted string); token counts are
   *  whole and well within double's exact-integer range (2^53). Stored as
   *  an integer server-side.
   */
  tokenLimit?: number | undefined;
};

/** @internal */
export const BudgetLimits$inboundSchema: z.ZodType<
  BudgetLimits,
  z.ZodTypeDef,
  unknown
> = z.object({
  period: BudgetPeriod$inboundSchema.optional(),
  amount: z.number().optional(),
  token_limit: z.number().optional(),
}).transform((v) => {
  return remap$(v, {
    "token_limit": "tokenLimit",
  });
});
/** @internal */
export type BudgetLimits$Outbound = {
  period?: string | undefined;
  amount?: number | undefined;
  token_limit?: number | undefined;
};

/** @internal */
export const BudgetLimits$outboundSchema: z.ZodType<
  BudgetLimits$Outbound,
  z.ZodTypeDef,
  BudgetLimits
> = z.object({
  period: BudgetPeriod$outboundSchema.optional(),
  amount: z.number().optional(),
  tokenLimit: z.number().optional(),
}).transform((v) => {
  return remap$(v, {
    tokenLimit: "token_limit",
  });
});

export function budgetLimitsToJSON(budgetLimits: BudgetLimits): string {
  return JSON.stringify(BudgetLimits$outboundSchema.parse(budgetLimits));
}
export function budgetLimitsFromJSON(
  jsonString: string,
): SafeParseResult<BudgetLimits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BudgetLimits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BudgetLimits' from JSON`,
  );
}
