/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import {
  collectExtraKeys as collectExtraKeys$,
  safeParse,
} from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A `response.output_item.done` server-sent event.
 */
export type ResponseOutputItemDoneStreamEvent = {
  /**
   * The output item (message, function call, reasoning, etc.).
   */
  item: { [k: string]: any };
  /**
   * Index of the output item in the response output array.
   */
  outputIndex: number;
  /**
   * Monotonically increasing sequence number for ordering events.
   */
  sequenceNumber: number;
  /**
   * The event type. Discriminates the payload.
   */
  type: "response.output_item.done";
  additionalProperties?: { [k: string]: any } | undefined;
};

/** @internal */
export const ResponseOutputItemDoneStreamEvent$inboundSchema: z.ZodType<
  ResponseOutputItemDoneStreamEvent,
  z.ZodTypeDef,
  unknown
> = collectExtraKeys$(
  z.object({
    item: z.record(z.any()),
    output_index: z.number().int(),
    sequence_number: z.number().int(),
    type: z.literal("response.output_item.done"),
  }).catchall(z.any()),
  "additionalProperties",
  true,
).transform((v) => {
  return remap$(v, {
    "output_index": "outputIndex",
    "sequence_number": "sequenceNumber",
  });
});

export function responseOutputItemDoneStreamEventFromJSON(
  jsonString: string,
): SafeParseResult<ResponseOutputItemDoneStreamEvent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ResponseOutputItemDoneStreamEvent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ResponseOutputItemDoneStreamEvent' from JSON`,
  );
}
