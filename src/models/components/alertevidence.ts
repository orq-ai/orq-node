/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type AlertEvidence = {
  /**
   * Trace the exemplar span belongs to.
   */
  traceId: string;
  /**
   * Exemplar span ID.
   */
  spanId: string;
  /**
   * Start time of the exemplar span.
   */
  startTime: Date;
  /**
   * Metric contribution of the exemplar (e.g. cost or duration).
   */
  value: number;
};

/** @internal */
export const AlertEvidence$inboundSchema: z.ZodType<
  AlertEvidence,
  z.ZodTypeDef,
  unknown
> = z.object({
  trace_id: z.string(),
  span_id: z.string(),
  start_time: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  value: z.number(),
}).transform((v) => {
  return remap$(v, {
    "trace_id": "traceId",
    "span_id": "spanId",
    "start_time": "startTime",
  });
});

export function alertEvidenceFromJSON(
  jsonString: string,
): SafeParseResult<AlertEvidence, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AlertEvidence$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AlertEvidence' from JSON`,
  );
}
