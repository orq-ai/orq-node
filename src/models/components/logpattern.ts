/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { FacetValue, FacetValue$inboundSchema } from "./facetvalue.js";

export type LogPattern = {
  /**
   * Stable hash of the normalized template.
   */
  id: string;
  template: string;
  /**
   * Number of sampled records assigned to this pattern. This is exact when
   *
   * @remarks
   *  meta.truncated is false.
   */
  count: string;
  /**
   * Percentage of sampled records assigned to this pattern, from 0 to 100.
   */
  percentage: number;
  firstSeen: Date;
  lastSeen: Date;
  /**
   * A bounded set of example record identifiers; raw bodies are not repeated.
   */
  sampleLogIds: Array<string>;
  /**
   * Top severity and service values observed inside this pattern.
   */
  severitySummary: Array<FacetValue>;
  serviceSummary: Array<FacetValue>;
  /**
   * Bounded literal fragment that can be sent through SearchLogs to find
   *
   * @remarks
   *  representative raw records for this normalized pattern.
   */
  suggestedQuery?: string | undefined;
};

/** @internal */
export const LogPattern$inboundSchema: z.ZodType<
  LogPattern,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  template: z.string(),
  count: z.string(),
  percentage: z.number(),
  first_seen: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  last_seen: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  sample_log_ids: z.array(z.string()),
  severity_summary: z.array(FacetValue$inboundSchema),
  service_summary: z.array(FacetValue$inboundSchema),
  suggested_query: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "first_seen": "firstSeen",
    "last_seen": "lastSeen",
    "sample_log_ids": "sampleLogIds",
    "severity_summary": "severitySummary",
    "service_summary": "serviceSummary",
    "suggested_query": "suggestedQuery",
  });
});

export function logPatternFromJSON(
  jsonString: string,
): SafeParseResult<LogPattern, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LogPattern$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LogPattern' from JSON`,
  );
}
