/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import {
  collectExtraKeys as collectExtraKeys$,
  safeParse,
} from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A `response.image_generation_call.in_progress` server-sent event.
 */
export type ResponseImageGenerationCallInProgressStreamEvent = {
  /**
   * ID of the output item this event refers to.
   */
  itemId: string;
  /**
   * Index of the output item in the response output array.
   */
  outputIndex: number;
  /**
   * Monotonically increasing sequence number for ordering events.
   */
  sequenceNumber: number;
  /**
   * The event type. Discriminates the payload.
   */
  type: "response.image_generation_call.in_progress";
  additionalProperties?: { [k: string]: any } | undefined;
};

/** @internal */
export const ResponseImageGenerationCallInProgressStreamEvent$inboundSchema:
  z.ZodType<
    ResponseImageGenerationCallInProgressStreamEvent,
    z.ZodTypeDef,
    unknown
  > = collectExtraKeys$(
    z.object({
      item_id: z.string(),
      output_index: z.number().int(),
      sequence_number: z.number().int(),
      type: z.literal("response.image_generation_call.in_progress"),
    }).catchall(z.any()),
    "additionalProperties",
    true,
  ).transform((v) => {
    return remap$(v, {
      "item_id": "itemId",
      "output_index": "outputIndex",
      "sequence_number": "sequenceNumber",
    });
  });

export function responseImageGenerationCallInProgressStreamEventFromJSON(
  jsonString: string,
): SafeParseResult<
  ResponseImageGenerationCallInProgressStreamEvent,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      ResponseImageGenerationCallInProgressStreamEvent$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'ResponseImageGenerationCallInProgressStreamEvent' from JSON`,
  );
}
