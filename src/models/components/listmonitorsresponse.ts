/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { Monitor, Monitor$inboundSchema } from "./monitor.js";

export type ListMonitorsResponse = {
  /**
   * Object discriminator for list responses; always `list`.
   */
  object: string;
  /**
   * Page of monitors, ordered newest first.
   */
  data: Array<Monitor>;
  /**
   * Whether more monitors are available in the selected pagination
   *
   * @remarks
   *  direction.
   */
  hasMore: boolean;
};

/** @internal */
export const ListMonitorsResponse$inboundSchema: z.ZodType<
  ListMonitorsResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  object: z.string(),
  data: z.array(Monitor$inboundSchema),
  has_more: z.boolean(),
}).transform((v) => {
  return remap$(v, {
    "has_more": "hasMore",
  });
});

export function listMonitorsResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListMonitorsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListMonitorsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListMonitorsResponse' from JSON`,
  );
}
