/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { AlertTrigger, AlertTrigger$inboundSchema } from "./alerttrigger.js";

export type ListAlertTriggersResponse = {
  /**
   * Object discriminator for list responses; always `list`.
   */
  object: string;
  /**
   * Page of triggers, ordered newest first.
   */
  data: Array<AlertTrigger>;
  /**
   * Whether more triggers are available in the selected pagination
   *
   * @remarks
   *  direction.
   */
  hasMore: boolean;
};

/** @internal */
export const ListAlertTriggersResponse$inboundSchema: z.ZodType<
  ListAlertTriggersResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  object: z.string(),
  data: z.array(AlertTrigger$inboundSchema),
  has_more: z.boolean(),
}).transform((v) => {
  return remap$(v, {
    "has_more": "hasMore",
  });
});

export function listAlertTriggersResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListAlertTriggersResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListAlertTriggersResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListAlertTriggersResponse' from JSON`,
  );
}
