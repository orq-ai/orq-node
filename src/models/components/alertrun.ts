/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Tier the value landed in when breached. Empty when not breached.
 */
export const Severity = {
  Critical: "critical",
  Degraded: "degraded",
} as const;
/**
 * Tier the value landed in when breached. Empty when not breached.
 */
export type Severity = ClosedEnum<typeof Severity>;

export type AlertRun = {
  /**
   * Time of the evaluation tick.
   */
  at: Date;
  /**
   * Observed metric value. Zero when `has_data` is false.
   */
  value: number;
  /**
   * Whether the value breached the alert condition.
   */
  breached: boolean;
  /**
   * Whether the evaluation window contained any data.
   */
  hasData: boolean;
  /**
   * Tier the value landed in when breached. Empty when not breached.
   */
  severity?: Severity | undefined;
};

/** @internal */
export const Severity$inboundSchema: z.ZodNativeEnum<typeof Severity> = z
  .nativeEnum(Severity);

/** @internal */
export const AlertRun$inboundSchema: z.ZodType<
  AlertRun,
  z.ZodTypeDef,
  unknown
> = z.object({
  at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  value: z.number(),
  breached: z.boolean(),
  has_data: z.boolean(),
  severity: Severity$inboundSchema.optional(),
}).transform((v) => {
  return remap$(v, {
    "has_data": "hasData",
  });
});

export function alertRunFromJSON(
  jsonString: string,
): SafeParseResult<AlertRun, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AlertRun$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AlertRun' from JSON`,
  );
}
