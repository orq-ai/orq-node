/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type KnowledgeMetadata = {
  wordCount?: number | undefined;
  documentCount?: number | undefined;
  sentencesCount?: number | undefined;
  supportEnabled?: boolean | undefined;
  uniqueMetadataFields?: Array<string> | undefined;
};

/** @internal */
export const KnowledgeMetadata$inboundSchema: z.ZodType<
  KnowledgeMetadata,
  z.ZodTypeDef,
  unknown
> = z.object({
  word_count: z.number().int().optional(),
  document_count: z.number().int().optional(),
  sentences_count: z.number().int().optional(),
  support_enabled: z.boolean().optional(),
  unique_metadata_fields: z.array(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    "word_count": "wordCount",
    "document_count": "documentCount",
    "sentences_count": "sentencesCount",
    "support_enabled": "supportEnabled",
    "unique_metadata_fields": "uniqueMetadataFields",
  });
});

export function knowledgeMetadataFromJSON(
  jsonString: string,
): SafeParseResult<KnowledgeMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => KnowledgeMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'KnowledgeMetadata' from JSON`,
  );
}
