/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { SyncStatus, SyncStatus$inboundSchema } from "./syncstatus.js";

export type McpSyncState = {
  status?: SyncStatus | undefined;
  toolsTotal?: number | undefined;
  toolsAdded?: number | undefined;
  toolsRemoved?: number | undefined;
  lastSyncedAt?: string | undefined;
  errors?: Array<string> | undefined;
};

/** @internal */
export const McpSyncState$inboundSchema: z.ZodType<
  McpSyncState,
  z.ZodTypeDef,
  unknown
> = z.object({
  status: SyncStatus$inboundSchema.optional(),
  tools_total: z.number().int().optional(),
  tools_added: z.number().int().optional(),
  tools_removed: z.number().int().optional(),
  last_synced_at: z.string().optional(),
  errors: z.array(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    "tools_total": "toolsTotal",
    "tools_added": "toolsAdded",
    "tools_removed": "toolsRemoved",
    "last_synced_at": "lastSyncedAt",
  });
});

export function mcpSyncStateFromJSON(
  jsonString: string,
): SafeParseResult<McpSyncState, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => McpSyncState$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'McpSyncState' from JSON`,
  );
}
