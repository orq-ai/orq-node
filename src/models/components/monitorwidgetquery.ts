/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  MonitorFilter,
  MonitorFilter$inboundSchema,
  MonitorFilter$Outbound,
  MonitorFilter$outboundSchema,
} from "./monitorfilter.js";

/**
 * Requested bucket grain for timeseries widgets.
 */
export const Grain = {
  Auto: "auto",
  Minute: "minute",
  Hour: "hour",
  Day: "day",
} as const;
/**
 * Requested bucket grain for timeseries widgets.
 */
export type Grain = ClosedEnum<typeof Grain>;

export type MonitorWidgetQuery = {
  /**
   * Reporting API catalogue metric, e.g. `genai.cost` or
   *
   * @remarks
   *  `genai.latency.p95`. Validated against the catalogue when the
   *  monitor is saved.
   */
  metric: string;
  /**
   * Reporting dimensions to break down by. Valid dimensions depend on
   *
   * @remarks
   *  the metric; validated when the monitor is saved.
   */
  groupBy?: Array<string> | undefined;
  /**
   * Reporting filters combined with AND.
   */
  filters?: Array<MonitorFilter> | undefined;
  /**
   * Requested bucket grain for timeseries widgets.
   */
  grain?: Grain | undefined;
  /**
   * Row cap. For toplist widgets this is the top-N group count.
   */
  limit?: number | undefined;
};

/** @internal */
export const Grain$inboundSchema: z.ZodNativeEnum<typeof Grain> = z.nativeEnum(
  Grain,
);
/** @internal */
export const Grain$outboundSchema: z.ZodNativeEnum<typeof Grain> =
  Grain$inboundSchema;

/** @internal */
export const MonitorWidgetQuery$inboundSchema: z.ZodType<
  MonitorWidgetQuery,
  z.ZodTypeDef,
  unknown
> = z.object({
  metric: z.string(),
  group_by: z.array(z.string()).optional(),
  filters: z.array(MonitorFilter$inboundSchema).optional(),
  grain: Grain$inboundSchema.optional(),
  limit: z.number().int().optional(),
}).transform((v) => {
  return remap$(v, {
    "group_by": "groupBy",
  });
});
/** @internal */
export type MonitorWidgetQuery$Outbound = {
  metric: string;
  group_by?: Array<string> | undefined;
  filters?: Array<MonitorFilter$Outbound> | undefined;
  grain?: string | undefined;
  limit?: number | undefined;
};

/** @internal */
export const MonitorWidgetQuery$outboundSchema: z.ZodType<
  MonitorWidgetQuery$Outbound,
  z.ZodTypeDef,
  MonitorWidgetQuery
> = z.object({
  metric: z.string(),
  groupBy: z.array(z.string()).optional(),
  filters: z.array(MonitorFilter$outboundSchema).optional(),
  grain: Grain$outboundSchema.optional(),
  limit: z.number().int().optional(),
}).transform((v) => {
  return remap$(v, {
    groupBy: "group_by",
  });
});

export function monitorWidgetQueryToJSON(
  monitorWidgetQuery: MonitorWidgetQuery,
): string {
  return JSON.stringify(
    MonitorWidgetQuery$outboundSchema.parse(monitorWidgetQuery),
  );
}
export function monitorWidgetQueryFromJSON(
  jsonString: string,
): SafeParseResult<MonitorWidgetQuery, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MonitorWidgetQuery$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MonitorWidgetQuery' from JSON`,
  );
}
