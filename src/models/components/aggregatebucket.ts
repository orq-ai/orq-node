/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type AggregateBucket = {
  timestamp?: Date | undefined;
  /**
   * severity_number → count
   */
  severityCounts?: { [k: string]: string } | undefined;
  totalCount?: string | undefined;
};

/** @internal */
export const AggregateBucket$inboundSchema: z.ZodType<
  AggregateBucket,
  z.ZodTypeDef,
  unknown
> = z.object({
  timestamp: z.string().datetime({ offset: true }).transform(v => new Date(v))
    .optional(),
  severity_counts: z.record(z.string()).optional(),
  total_count: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "severity_counts": "severityCounts",
    "total_count": "totalCount",
  });
});

export function aggregateBucketFromJSON(
  jsonString: string,
): SafeParseResult<AggregateBucket, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AggregateBucket$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AggregateBucket' from JSON`,
  );
}
