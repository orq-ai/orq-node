/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type ListGuardrailRuleUsedGuardrailsResponse = {
  object: string;
  guardrailIds: Array<string>;
};

/** @internal */
export const ListGuardrailRuleUsedGuardrailsResponse$inboundSchema: z.ZodType<
  ListGuardrailRuleUsedGuardrailsResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  object: z.string(),
  guardrail_ids: z.array(z.string()),
}).transform((v) => {
  return remap$(v, {
    "guardrail_ids": "guardrailIds",
  });
});

export function listGuardrailRuleUsedGuardrailsResponseFromJSON(
  jsonString: string,
): SafeParseResult<
  ListGuardrailRuleUsedGuardrailsResponse,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      ListGuardrailRuleUsedGuardrailsResponse$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'ListGuardrailRuleUsedGuardrailsResponse' from JSON`,
  );
}
