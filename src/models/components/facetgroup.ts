/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { FacetKey, FacetKey$inboundSchema } from "./facetkey.js";

/**
 * FacetGroup is one attribute family in the facet hierarchy. name is one of
 *
 * @remarks
 *  "native" (promoted columns), "attribute" (log_attributes), "resource"
 *  (resource_attributes), or "scope" (scope_attributes).
 */
export type FacetGroup = {
  name?: string | undefined;
  keys?: Array<FacetKey> | undefined;
};

/** @internal */
export const FacetGroup$inboundSchema: z.ZodType<
  FacetGroup,
  z.ZodTypeDef,
  unknown
> = z.object({
  name: z.string().optional(),
  keys: z.array(FacetKey$inboundSchema).optional(),
});

export function facetGroupFromJSON(
  jsonString: string,
): SafeParseResult<FacetGroup, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FacetGroup$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FacetGroup' from JSON`,
  );
}
