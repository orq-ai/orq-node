/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { FileEntry, FileEntry$inboundSchema } from "./fileentry.js";

export type ListFileSystemFilesResponse = {
  /**
   * Folder that was listed, relative to the file system root
   */
  path: string;
  /**
   * Number of folder levels that were walked
   */
  depth: number;
  entries: Array<FileEntry>;
  /**
   * Number of entries returned
   */
  entryCount: number;
  /**
   * Whether the listing stopped at max_entries
   */
  truncated: boolean;
};

/** @internal */
export const ListFileSystemFilesResponse$inboundSchema: z.ZodType<
  ListFileSystemFilesResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  path: z.string(),
  depth: z.number().int(),
  entries: z.array(FileEntry$inboundSchema),
  entry_count: z.number().int(),
  truncated: z.boolean(),
}).transform((v) => {
  return remap$(v, {
    "entry_count": "entryCount",
  });
});

export function listFileSystemFilesResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListFileSystemFilesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListFileSystemFilesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListFileSystemFilesResponse' from JSON`,
  );
}
