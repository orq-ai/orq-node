/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Whether the breach is ongoing or recovered.
 */
export const AlertTriggerStatus = {
  Open: "open",
  Resolved: "resolved",
} as const;
/**
 * Whether the breach is ongoing or recovered.
 */
export type AlertTriggerStatus = ClosedEnum<typeof AlertTriggerStatus>;

/**
 * Current tier of the incident.
 */
export const AlertTriggerSeverity = {
  Critical: "critical",
  Degraded: "degraded",
} as const;
/**
 * Current tier of the incident.
 */
export type AlertTriggerSeverity = ClosedEnum<typeof AlertTriggerSeverity>;

/**
 * Worst tier reached while the trigger was open.
 */
export const PeakSeverity = {
  Critical: "critical",
  Degraded: "degraded",
} as const;
/**
 * Worst tier reached while the trigger was open.
 */
export type PeakSeverity = ClosedEnum<typeof PeakSeverity>;

export type AlertTrigger = {
  /**
   * Unique trigger identifier, for example `trigger_01H...`.
   */
  triggerId: string;
  /**
   * Alert this trigger belongs to.
   */
  alertId: string;
  /**
   * Project that owns the alert.
   */
  projectId: string;
  /**
   * Whether the breach is ongoing or recovered.
   */
  status: AlertTriggerStatus;
  /**
   * Current tier of the incident.
   */
  severity?: AlertTriggerSeverity | undefined;
  /**
   * Worst tier reached while the trigger was open.
   */
  peakSeverity?: PeakSeverity | undefined;
  /**
   * Time when the threshold was first crossed.
   */
  openedAt: Date;
  /**
   * Time when the value recovered. Unset while the trigger is open.
   */
  resolvedAt?: Date | undefined;
  /**
   * Worst observed value while the trigger was open.
   */
  peakValue: number;
  /**
   * Most recent observed value.
   */
  lastValue: number;
  /**
   * Number of evaluation events recorded on this trigger.
   */
  eventCount: number;
};

/** @internal */
export const AlertTriggerStatus$inboundSchema: z.ZodNativeEnum<
  typeof AlertTriggerStatus
> = z.nativeEnum(AlertTriggerStatus);

/** @internal */
export const AlertTriggerSeverity$inboundSchema: z.ZodNativeEnum<
  typeof AlertTriggerSeverity
> = z.nativeEnum(AlertTriggerSeverity);

/** @internal */
export const PeakSeverity$inboundSchema: z.ZodNativeEnum<typeof PeakSeverity> =
  z.nativeEnum(PeakSeverity);

/** @internal */
export const AlertTrigger$inboundSchema: z.ZodType<
  AlertTrigger,
  z.ZodTypeDef,
  unknown
> = z.object({
  trigger_id: z.string(),
  alert_id: z.string(),
  project_id: z.string(),
  status: AlertTriggerStatus$inboundSchema,
  severity: AlertTriggerSeverity$inboundSchema.optional(),
  peak_severity: PeakSeverity$inboundSchema.optional(),
  opened_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  resolved_at: z.string().datetime({ offset: true }).transform(v => new Date(v))
    .optional(),
  peak_value: z.number(),
  last_value: z.number(),
  event_count: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    "trigger_id": "triggerId",
    "alert_id": "alertId",
    "project_id": "projectId",
    "peak_severity": "peakSeverity",
    "opened_at": "openedAt",
    "resolved_at": "resolvedAt",
    "peak_value": "peakValue",
    "last_value": "lastValue",
    "event_count": "eventCount",
  });
});

export function alertTriggerFromJSON(
  jsonString: string,
): SafeParseResult<AlertTrigger, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AlertTrigger$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AlertTrigger' from JSON`,
  );
}
