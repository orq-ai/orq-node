/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";

export type ResponseRetryConfig = {
  /**
   * Number of retries (1-5).
   */
  count: number;
  /**
   * HTTP status codes that trigger a retry (e.g. [429, 500, 502, 503]).
   */
  onCodes: Array<number> | null;
};

/** @internal */
export type ResponseRetryConfig$Outbound = {
  count: number;
  on_codes: Array<number> | null;
};

/** @internal */
export const ResponseRetryConfig$outboundSchema: z.ZodType<
  ResponseRetryConfig$Outbound,
  z.ZodTypeDef,
  ResponseRetryConfig
> = z.object({
  count: z.number().int(),
  onCodes: z.nullable(z.array(z.number().int())),
}).transform((v) => {
  return remap$(v, {
    onCodes: "on_codes",
  });
});

export function responseRetryConfigToJSON(
  responseRetryConfig: ResponseRetryConfig,
): string {
  return JSON.stringify(
    ResponseRetryConfig$outboundSchema.parse(responseRetryConfig),
  );
}
