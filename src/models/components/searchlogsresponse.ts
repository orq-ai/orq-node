/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { Log, Log$inboundSchema } from "./log.js";
import {
  TraceSearchMeta,
  TraceSearchMeta$inboundSchema,
} from "./tracesearchmeta.js";

export type SearchLogsResponse = {
  /**
   * Object discriminator; always "list". Mirrors SearchTracesResponse.
   */
  object?: string | undefined;
  data?: Array<Log> | undefined;
  hasMore?: boolean | undefined;
  /**
   * Keyset cursor for the next page; empty when has_more is false.
   */
  nextPageToken?: string | undefined;
  meta?: TraceSearchMeta | undefined;
  /**
   * Exact match count for the full time range (cursor-independent, constant
   *
   * @remarks
   *  across pages). int64 serializes as a JSON string per protojson.
   */
  totalCount?: string | undefined;
};

/** @internal */
export const SearchLogsResponse$inboundSchema: z.ZodType<
  SearchLogsResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  object: z.string().optional(),
  data: z.array(Log$inboundSchema).optional(),
  has_more: z.boolean().optional(),
  next_page_token: z.string().optional(),
  meta: TraceSearchMeta$inboundSchema.optional(),
  total_count: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "has_more": "hasMore",
    "next_page_token": "nextPageToken",
    "total_count": "totalCount",
  });
});

export function searchLogsResponseFromJSON(
  jsonString: string,
): SafeParseResult<SearchLogsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchLogsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchLogsResponse' from JSON`,
  );
}
