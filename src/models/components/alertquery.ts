/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  MonitorFilter,
  MonitorFilter$inboundSchema,
  MonitorFilter$Outbound,
  MonitorFilter$outboundSchema,
} from "./monitorfilter.js";

export type AlertQuery = {
  /**
   * Reporting API catalogue metric, e.g. `genai.cost` or
   *
   * @remarks
   *  `genai.latency.avg`. Validated against the catalogue when the alert
   *  is saved.
   */
  metric: string;
  /**
   * Reporting filters combined with AND.
   */
  filters?: Array<MonitorFilter> | undefined;
};

/** @internal */
export const AlertQuery$inboundSchema: z.ZodType<
  AlertQuery,
  z.ZodTypeDef,
  unknown
> = z.object({
  metric: z.string(),
  filters: z.array(MonitorFilter$inboundSchema).optional(),
});
/** @internal */
export type AlertQuery$Outbound = {
  metric: string;
  filters?: Array<MonitorFilter$Outbound> | undefined;
};

/** @internal */
export const AlertQuery$outboundSchema: z.ZodType<
  AlertQuery$Outbound,
  z.ZodTypeDef,
  AlertQuery
> = z.object({
  metric: z.string(),
  filters: z.array(MonitorFilter$outboundSchema).optional(),
});

export function alertQueryToJSON(alertQuery: AlertQuery): string {
  return JSON.stringify(AlertQuery$outboundSchema.parse(alertQuery));
}
export function alertQueryFromJSON(
  jsonString: string,
): SafeParseResult<AlertQuery, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AlertQuery$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AlertQuery' from JSON`,
  );
}
