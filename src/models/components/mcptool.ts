/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  McpToolAnnotations,
  McpToolAnnotations$inboundSchema,
} from "./mcptoolannotations.js";

/**
 * JSON Schema for the tool arguments, as advertised upstream.
 */
export type McpToolInputSchema = {};

export type McpTool = {
  /**
   * Bare tool name as advertised by the upstream server.
   */
  name?: string | undefined;
  /**
   * Tool description advertised upstream, passed through to MCP clients.
   */
  description?: string | undefined;
  /**
   * JSON Schema for the tool arguments, as advertised upstream.
   */
  inputSchema?: McpToolInputSchema | undefined;
  /**
   * `ACTIVE`, or `MISSING` when the last sync no longer found the tool upstream.
   */
  status?: string | undefined;
  /**
   * Assigned once on first discovery and preserved across sync.
   */
  id?: string | undefined;
  /**
   * Human-readable label advertised upstream, when the server sends one.
   */
  title?: string | undefined;
  /**
   * Behaviour hints claimed by the upstream server.
   */
  annotations?: McpToolAnnotations | undefined;
};

/** @internal */
export const McpToolInputSchema$inboundSchema: z.ZodType<
  McpToolInputSchema,
  z.ZodTypeDef,
  unknown
> = z.object({});

export function mcpToolInputSchemaFromJSON(
  jsonString: string,
): SafeParseResult<McpToolInputSchema, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => McpToolInputSchema$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'McpToolInputSchema' from JSON`,
  );
}

/** @internal */
export const McpTool$inboundSchema: z.ZodType<McpTool, z.ZodTypeDef, unknown> =
  z.object({
    name: z.string().optional(),
    description: z.string().optional(),
    input_schema: z.lazy(() => McpToolInputSchema$inboundSchema).optional(),
    status: z.string().optional(),
    id: z.string().optional(),
    title: z.string().optional(),
    annotations: McpToolAnnotations$inboundSchema.optional(),
  }).transform((v) => {
    return remap$(v, {
      "input_schema": "inputSchema",
    });
  });

export function mcpToolFromJSON(
  jsonString: string,
): SafeParseResult<McpTool, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => McpTool$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'McpTool' from JSON`,
  );
}
