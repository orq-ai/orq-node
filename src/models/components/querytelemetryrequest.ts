/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  TelemetrySource,
  TelemetrySource$inboundSchema,
  TelemetrySource$outboundSchema,
} from "./telemetrysource.js";
import {
  TraceCompute,
  TraceCompute$inboundSchema,
  TraceCompute$Outbound,
  TraceCompute$outboundSchema,
} from "./tracecompute.js";
import {
  TraceFilter,
  TraceFilter$inboundSchema,
  TraceFilter$Outbound,
  TraceFilter$outboundSchema,
} from "./tracefilter.js";

/**
 * Value shaping. `timeseries` buckets by grain; `scalar` returns one row per group. When omitted, grain selects the compatible shape.
 */
export const QueryTelemetryRequestMode = {
  Timeseries: "timeseries",
  Scalar: "scalar",
} as const;
/**
 * Value shaping. `timeseries` buckets by grain; `scalar` returns one row per group. When omitted, grain selects the compatible shape.
 */
export type QueryTelemetryRequestMode = ClosedEnum<
  typeof QueryTelemetryRequestMode
>;

/**
 * Ordering for scalar/top-list rows. Defaults to `desc`.
 */
export const QueryTelemetryRequestSort = {
  Desc: "desc",
  Asc: "asc",
} as const;
/**
 * Ordering for scalar/top-list rows. Defaults to `desc`.
 */
export type QueryTelemetryRequestSort = ClosedEnum<
  typeof QueryTelemetryRequestSort
>;

export type QueryTelemetryRequest = {
  source?: TelemetrySource | undefined;
  from?: Date | undefined;
  to?: Date | undefined;
  compute?: Array<TraceCompute> | undefined;
  /**
   * Empty string and "none" are equivalent: a single row per group instead
   *
   * @remarks
   *  of a time series.
   */
  grain?: string | undefined;
  groupBy?: Array<string> | undefined;
  filters?: Array<TraceFilter> | undefined;
  filterOperator?: string | undefined;
  /**
   * Scalar queries: maximum returned rows, default 100. Time-series queries: optional maximum number of distinct groups; exceeding it fails the query instead of truncating timelines. Time-series results are complete within safety budgets of 5000 rows, 5000 buckets, and 50000 metric values. Queries are limited to 90 days and 30 seconds of execution.
   */
  limit?: number | undefined;
  timeZone?: string | undefined;
  includeTotals?: boolean | undefined;
  /**
   * Value shaping. `timeseries` buckets by grain; `scalar` returns one row per group. When omitted, grain selects the compatible shape.
   */
  mode?: QueryTelemetryRequestMode | undefined;
  /**
   * Ordering for scalar/top-list rows. Defaults to `desc`.
   */
  sort?: QueryTelemetryRequestSort | undefined;
  /**
   * Explicit bucket width in seconds. Takes precedence over `grain` when both are set.
   */
  intervalSeconds?: number | undefined;
  /**
   * The span originally selected, before live extended [from, to). `grain:"auto"` resolves bucket width from this span, independent of how far [from, to) has since grown.
   */
  selectedRangeSeconds?: number | undefined;
};

/** @internal */
export const QueryTelemetryRequestMode$inboundSchema: z.ZodNativeEnum<
  typeof QueryTelemetryRequestMode
> = z.nativeEnum(QueryTelemetryRequestMode);
/** @internal */
export const QueryTelemetryRequestMode$outboundSchema: z.ZodNativeEnum<
  typeof QueryTelemetryRequestMode
> = QueryTelemetryRequestMode$inboundSchema;

/** @internal */
export const QueryTelemetryRequestSort$inboundSchema: z.ZodNativeEnum<
  typeof QueryTelemetryRequestSort
> = z.nativeEnum(QueryTelemetryRequestSort);
/** @internal */
export const QueryTelemetryRequestSort$outboundSchema: z.ZodNativeEnum<
  typeof QueryTelemetryRequestSort
> = QueryTelemetryRequestSort$inboundSchema;

/** @internal */
export const QueryTelemetryRequest$inboundSchema: z.ZodType<
  QueryTelemetryRequest,
  z.ZodTypeDef,
  unknown
> = z.object({
  source: TelemetrySource$inboundSchema.optional(),
  from: z.string().datetime({ offset: true }).transform(v => new Date(v))
    .optional(),
  to: z.string().datetime({ offset: true }).transform(v => new Date(v))
    .optional(),
  compute: z.array(TraceCompute$inboundSchema).optional(),
  grain: z.string().optional(),
  group_by: z.array(z.string()).optional(),
  filters: z.array(TraceFilter$inboundSchema).optional(),
  filter_operator: z.string().optional(),
  limit: z.number().int().optional(),
  time_zone: z.string().optional(),
  include_totals: z.boolean().optional(),
  mode: QueryTelemetryRequestMode$inboundSchema.optional(),
  sort: QueryTelemetryRequestSort$inboundSchema.optional(),
  interval_seconds: z.number().int().optional(),
  selected_range_seconds: z.number().int().optional(),
}).transform((v) => {
  return remap$(v, {
    "group_by": "groupBy",
    "filter_operator": "filterOperator",
    "time_zone": "timeZone",
    "include_totals": "includeTotals",
    "interval_seconds": "intervalSeconds",
    "selected_range_seconds": "selectedRangeSeconds",
  });
});
/** @internal */
export type QueryTelemetryRequest$Outbound = {
  source?: string | undefined;
  from?: string | undefined;
  to?: string | undefined;
  compute?: Array<TraceCompute$Outbound> | undefined;
  grain?: string | undefined;
  group_by?: Array<string> | undefined;
  filters?: Array<TraceFilter$Outbound> | undefined;
  filter_operator?: string | undefined;
  limit?: number | undefined;
  time_zone?: string | undefined;
  include_totals?: boolean | undefined;
  mode?: string | undefined;
  sort?: string | undefined;
  interval_seconds?: number | undefined;
  selected_range_seconds?: number | undefined;
};

/** @internal */
export const QueryTelemetryRequest$outboundSchema: z.ZodType<
  QueryTelemetryRequest$Outbound,
  z.ZodTypeDef,
  QueryTelemetryRequest
> = z.object({
  source: TelemetrySource$outboundSchema.optional(),
  from: z.date().transform(v => v.toISOString()).optional(),
  to: z.date().transform(v => v.toISOString()).optional(),
  compute: z.array(TraceCompute$outboundSchema).optional(),
  grain: z.string().optional(),
  groupBy: z.array(z.string()).optional(),
  filters: z.array(TraceFilter$outboundSchema).optional(),
  filterOperator: z.string().optional(),
  limit: z.number().int().optional(),
  timeZone: z.string().optional(),
  includeTotals: z.boolean().optional(),
  mode: QueryTelemetryRequestMode$outboundSchema.optional(),
  sort: QueryTelemetryRequestSort$outboundSchema.optional(),
  intervalSeconds: z.number().int().optional(),
  selectedRangeSeconds: z.number().int().optional(),
}).transform((v) => {
  return remap$(v, {
    groupBy: "group_by",
    filterOperator: "filter_operator",
    timeZone: "time_zone",
    includeTotals: "include_totals",
    intervalSeconds: "interval_seconds",
    selectedRangeSeconds: "selected_range_seconds",
  });
});

export function queryTelemetryRequestToJSON(
  queryTelemetryRequest: QueryTelemetryRequest,
): string {
  return JSON.stringify(
    QueryTelemetryRequest$outboundSchema.parse(queryTelemetryRequest),
  );
}
export function queryTelemetryRequestFromJSON(
  jsonString: string,
): SafeParseResult<QueryTelemetryRequest, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => QueryTelemetryRequest$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'QueryTelemetryRequest' from JSON`,
  );
}
