/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  ProcessingError,
  ProcessingError$inboundSchema,
} from "./processingerror.js";

export type ProcessingAttempt = {
  id: string;
  startedAt: string;
  queuedAt?: string | undefined;
  completedAt?: string | undefined;
  errors?: Array<ProcessingError> | undefined;
  retryable?: boolean | undefined;
};

/** @internal */
export const ProcessingAttempt$inboundSchema: z.ZodType<
  ProcessingAttempt,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  started_at: z.string(),
  queued_at: z.string().optional(),
  completed_at: z.string().optional(),
  errors: z.array(ProcessingError$inboundSchema).optional(),
  retryable: z.boolean().optional(),
}).transform((v) => {
  return remap$(v, {
    "started_at": "startedAt",
    "queued_at": "queuedAt",
    "completed_at": "completedAt",
  });
});

export function processingAttemptFromJSON(
  jsonString: string,
): SafeParseResult<ProcessingAttempt, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ProcessingAttempt$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ProcessingAttempt' from JSON`,
  );
}
