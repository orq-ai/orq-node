/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type ThreadUsage = {
  promptTokens: number;
  completionTokens: number;
  totalTokens: number;
};

/** @internal */
export const ThreadUsage$inboundSchema: z.ZodType<
  ThreadUsage,
  z.ZodTypeDef,
  unknown
> = z.object({
  prompt_tokens: z.number().int(),
  completion_tokens: z.number().int(),
  total_tokens: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    "prompt_tokens": "promptTokens",
    "completion_tokens": "completionTokens",
    "total_tokens": "totalTokens",
  });
});
/** @internal */
export type ThreadUsage$Outbound = {
  prompt_tokens: number;
  completion_tokens: number;
  total_tokens: number;
};

/** @internal */
export const ThreadUsage$outboundSchema: z.ZodType<
  ThreadUsage$Outbound,
  z.ZodTypeDef,
  ThreadUsage
> = z.object({
  promptTokens: z.number().int(),
  completionTokens: z.number().int(),
  totalTokens: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    promptTokens: "prompt_tokens",
    completionTokens: "completion_tokens",
    totalTokens: "total_tokens",
  });
});

export function threadUsageToJSON(threadUsage: ThreadUsage): string {
  return JSON.stringify(ThreadUsage$outboundSchema.parse(threadUsage));
}
export function threadUsageFromJSON(
  jsonString: string,
): SafeParseResult<ThreadUsage, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ThreadUsage$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ThreadUsage' from JSON`,
  );
}
