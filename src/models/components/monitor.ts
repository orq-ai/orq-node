/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { MonitorWidget, MonitorWidget$inboundSchema } from "./monitorwidget.js";

/**
 * Default relative time range applied when the monitor opens.
 */
export const DefaultRange = {
  Fifteenm: "15m",
  Oneh: "1h",
  Fourh: "4h",
  Oned: "1d",
  Threed: "3d",
  Sevend: "7d",
  Fourteend: "14d",
  Thirtyd: "30d",
  Ninetyd: "90d",
} as const;
/**
 * Default relative time range applied when the monitor opens.
 */
export type DefaultRange = ClosedEnum<typeof DefaultRange>;

export type Monitor = {
  /**
   * Unique monitor identifier assigned by ORQ, for example
   *
   * @remarks
   *  `monitor_01H...`. Use this value in retrieve, update, and delete
   *  requests.
   */
  monitorId: string;
  /**
   * Workspace-unique display name.
   */
  displayName: string;
  /**
   * Short human-readable summary of what the monitor tracks.
   */
  description: string;
  /**
   * Project that owns the monitor. Empty for workspace-wide monitors (admin-managed). Immutable after creation.
   */
  projectId: string;
  /**
   * Widgets rendered on the monitor, each carrying its own query and
   *
   * @remarks
   *  layout position.
   */
  widgets: Array<MonitorWidget>;
  /**
   * Default relative time range applied when the monitor opens.
   */
  defaultRange?: DefaultRange | undefined;
  /**
   * Preset this monitor was created from. Empty when built from scratch.
   */
  presetKey: string;
  /**
   * Time when the monitor was created.
   */
  createdAt: Date;
  /**
   * Time when the monitor was last updated.
   */
  updatedAt: Date;
  /**
   * User ID that created the monitor.
   */
  createdById: string;
  /**
   * User ID that last updated the monitor.
   */
  updatedById: string;
};

/** @internal */
export const DefaultRange$inboundSchema: z.ZodNativeEnum<typeof DefaultRange> =
  z.nativeEnum(DefaultRange);

/** @internal */
export const Monitor$inboundSchema: z.ZodType<Monitor, z.ZodTypeDef, unknown> =
  z.object({
    monitor_id: z.string(),
    display_name: z.string(),
    description: z.string(),
    project_id: z.string(),
    widgets: z.array(MonitorWidget$inboundSchema),
    default_range: DefaultRange$inboundSchema.optional(),
    preset_key: z.string(),
    created_at: z.string().datetime({ offset: true }).transform(v =>
      new Date(v)
    ),
    updated_at: z.string().datetime({ offset: true }).transform(v =>
      new Date(v)
    ),
    created_by_id: z.string(),
    updated_by_id: z.string(),
  }).transform((v) => {
    return remap$(v, {
      "monitor_id": "monitorId",
      "display_name": "displayName",
      "project_id": "projectId",
      "default_range": "defaultRange",
      "preset_key": "presetKey",
      "created_at": "createdAt",
      "updated_at": "updatedAt",
      "created_by_id": "createdById",
      "updated_by_id": "updatedById",
    });
  });

export function monitorFromJSON(
  jsonString: string,
): SafeParseResult<Monitor, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Monitor$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Monitor' from JSON`,
  );
}
