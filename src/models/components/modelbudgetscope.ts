/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Per-model cap. The value is the FULL model reference as callers send
 *
 * @remarks
 *  it ("openai/gpt-4o", or "workspaceKey@openai/gpt-4o" for private
 *  models) — NOT the Mongo `_id` of the model master-data document.
 */
export type ModelBudgetScope = {
  modelId?: string | undefined;
};

/** @internal */
export const ModelBudgetScope$inboundSchema: z.ZodType<
  ModelBudgetScope,
  z.ZodTypeDef,
  unknown
> = z.object({
  model_id: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "model_id": "modelId",
  });
});
/** @internal */
export type ModelBudgetScope$Outbound = {
  model_id?: string | undefined;
};

/** @internal */
export const ModelBudgetScope$outboundSchema: z.ZodType<
  ModelBudgetScope$Outbound,
  z.ZodTypeDef,
  ModelBudgetScope
> = z.object({
  modelId: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    modelId: "model_id",
  });
});

export function modelBudgetScopeToJSON(
  modelBudgetScope: ModelBudgetScope,
): string {
  return JSON.stringify(
    ModelBudgetScope$outboundSchema.parse(modelBudgetScope),
  );
}
export function modelBudgetScopeFromJSON(
  jsonString: string,
): SafeParseResult<ModelBudgetScope, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ModelBudgetScope$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ModelBudgetScope' from JSON`,
  );
}
