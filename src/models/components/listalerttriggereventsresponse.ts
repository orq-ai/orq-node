/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  AlertTriggerEvent,
  AlertTriggerEvent$inboundSchema,
} from "./alerttriggerevent.js";

export type ListAlertTriggerEventsResponse = {
  /**
   * Object discriminator for list responses; always `list`.
   */
  object: string;
  /**
   * Page of events, ordered newest first.
   */
  data: Array<AlertTriggerEvent>;
  /**
   * Whether more events are available in the selected pagination
   *
   * @remarks
   *  direction.
   */
  hasMore: boolean;
};

/** @internal */
export const ListAlertTriggerEventsResponse$inboundSchema: z.ZodType<
  ListAlertTriggerEventsResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  object: z.string(),
  data: z.array(AlertTriggerEvent$inboundSchema),
  has_more: z.boolean(),
}).transform((v) => {
  return remap$(v, {
    "has_more": "hasMore",
  });
});

export function listAlertTriggerEventsResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListAlertTriggerEventsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListAlertTriggerEventsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListAlertTriggerEventsResponse' from JSON`,
  );
}
