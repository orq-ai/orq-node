/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type ChunkMetadata = {
  startIndex: number;
  endIndex: number;
  tokenCount: number;
};

/** @internal */
export const ChunkMetadata$inboundSchema: z.ZodType<
  ChunkMetadata,
  z.ZodTypeDef,
  unknown
> = z.object({
  start_index: z.number().int(),
  end_index: z.number().int(),
  token_count: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    "start_index": "startIndex",
    "end_index": "endIndex",
    "token_count": "tokenCount",
  });
});

export function chunkMetadataFromJSON(
  jsonString: string,
): SafeParseResult<ChunkMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ChunkMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ChunkMetadata' from JSON`,
  );
}
