/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A newly generated webhook signing secret.
 */
export type GenerateWebhookSecretResponse = {
  /**
   * Signing secret to supply when creating or updating a webhook. Store it securely.
   */
  secret: string;
};

/** @internal */
export const GenerateWebhookSecretResponse$inboundSchema: z.ZodType<
  GenerateWebhookSecretResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  secret: z.string(),
});

export function generateWebhookSecretResponseFromJSON(
  jsonString: string,
): SafeParseResult<GenerateWebhookSecretResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GenerateWebhookSecretResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GenerateWebhookSecretResponse' from JSON`,
  );
}
