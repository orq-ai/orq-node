/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Hints claimed by the upstream server; the gateway does not enforce them.
 */
export type McpToolAnnotations = {
  /**
   * Upstream claims the tool does not modify state.
   */
  readOnly?: boolean | undefined;
  /**
   * Upstream claims the tool can perform destructive updates.
   */
  destructive?: boolean | undefined;
  /**
   * Upstream claims repeated calls with the same arguments have no extra effect.
   */
  idempotent?: boolean | undefined;
  /**
   * Upstream claims the tool reaches systems outside its own domain.
   */
  openWorld?: boolean | undefined;
};

/** @internal */
export const McpToolAnnotations$inboundSchema: z.ZodType<
  McpToolAnnotations,
  z.ZodTypeDef,
  unknown
> = z.object({
  read_only: z.boolean().optional(),
  destructive: z.boolean().optional(),
  idempotent: z.boolean().optional(),
  open_world: z.boolean().optional(),
}).transform((v) => {
  return remap$(v, {
    "read_only": "readOnly",
    "open_world": "openWorld",
  });
});

export function mcpToolAnnotationsFromJSON(
  jsonString: string,
): SafeParseResult<McpToolAnnotations, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => McpToolAnnotations$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'McpToolAnnotations' from JSON`,
  );
}
