/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { Notifier, Notifier$inboundSchema } from "./notifier.js";

export type ListNotifiersResponse = {
  /**
   * Object discriminator for list responses; always `list`.
   */
  object: string;
  /**
   * Page of notifiers.
   */
  data: Array<Notifier>;
  /**
   * Whether more notifiers are available in the selected pagination direction.
   */
  hasMore: boolean;
};

/** @internal */
export const ListNotifiersResponse$inboundSchema: z.ZodType<
  ListNotifiersResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  object: z.string(),
  data: z.array(Notifier$inboundSchema),
  has_more: z.boolean(),
}).transform((v) => {
  return remap$(v, {
    "has_more": "hasMore",
  });
});

export function listNotifiersResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListNotifiersResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListNotifiersResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListNotifiersResponse' from JSON`,
  );
}
