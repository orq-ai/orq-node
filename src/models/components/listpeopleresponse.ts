/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { Person, Person$inboundSchema } from "./person.js";

export type ListPeopleResponse = {
  /**
   * Object discriminator for list responses; always `list`.
   */
  object: string;
  /**
   * Page of people, ordered newest first.
   */
  data: Array<Person>;
  /**
   * Whether more people are available in the selected pagination
   *
   * @remarks
   *  direction.
   */
  hasMore: boolean;
};

/** @internal */
export const ListPeopleResponse$inboundSchema: z.ZodType<
  ListPeopleResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  object: z.string(),
  data: z.array(Person$inboundSchema),
  has_more: z.boolean(),
}).transform((v) => {
  return remap$(v, {
    "has_more": "hasMore",
  });
});

export function listPeopleResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListPeopleResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPeopleResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPeopleResponse' from JSON`,
  );
}
