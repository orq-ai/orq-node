/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { Alert, Alert$inboundSchema } from "./alert.js";

export type ListAlertsResponse = {
  /**
   * Object discriminator for list responses; always `list`.
   */
  object: string;
  /**
   * Page of alerts, ordered newest first.
   */
  data: Array<Alert>;
  /**
   * Whether more alerts are available in the selected pagination
   *
   * @remarks
   *  direction.
   */
  hasMore: boolean;
};

/** @internal */
export const ListAlertsResponse$inboundSchema: z.ZodType<
  ListAlertsResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  object: z.string(),
  data: z.array(Alert$inboundSchema),
  has_more: z.boolean(),
}).transform((v) => {
  return remap$(v, {
    "has_more": "hasMore",
  });
});

export function listAlertsResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListAlertsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListAlertsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListAlertsResponse' from JSON`,
  );
}
