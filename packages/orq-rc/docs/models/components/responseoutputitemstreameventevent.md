# ResponseOutputItemStreamEventEvent

The SSE event name, equal to the payload's `type`.

## Example Usage

```typescript
import { ResponseOutputItemStreamEventEvent } from "@orq-ai/node/models/components";

let value: ResponseOutputItemStreamEventEvent = "response.output_item.done";
```

## Values

```typescript
"response.output_item.added" | "response.output_item.done"
```