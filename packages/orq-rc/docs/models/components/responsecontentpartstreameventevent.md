# ResponseContentPartStreamEventEvent

The SSE event name, equal to the payload's `type`.

## Example Usage

```typescript
import { ResponseContentPartStreamEventEvent } from "@orq-ai/node/models/components";

let value: ResponseContentPartStreamEventEvent = "response.content_part.done";
```

## Values

```typescript
"response.content_part.added" | "response.content_part.done"
```