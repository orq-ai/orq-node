# ResponseReasoningDoneStreamEventEvent

The SSE event name, equal to the payload's `type`.

## Example Usage

```typescript
import { ResponseReasoningDoneStreamEventEvent } from "@orq-ai/node/models/components";

let value: ResponseReasoningDoneStreamEventEvent = "response.reasoning.done";
```

## Values

```typescript
"response.reasoning.done" | "response.reasoning_text.done"
```