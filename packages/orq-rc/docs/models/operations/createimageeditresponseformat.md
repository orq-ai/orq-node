# CreateImageEditResponseFormat

The format in which the generated images are returned. Some of the models only return the image in base64 format.

## Example Usage

```typescript
import { CreateImageEditResponseFormat } from "@orq-ai/node/models/operations";

let value: CreateImageEditResponseFormat = "url";
```

## Values

```typescript
"url" | "b64_json"
```