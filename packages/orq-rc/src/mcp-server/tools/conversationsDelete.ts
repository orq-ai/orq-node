/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { conversationsDelete } from "../../funcs/conversationsDelete.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  conversationId: z.string(),
};

export const tool$conversationsDelete: ToolDefinition<typeof args> = {
  name: "conversations-delete",
  description: `Delete conversation

Permanently removes a conversation from the workspace`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await conversationsDelete(
      client,
      args.conversationId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
