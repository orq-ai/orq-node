/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsUpdate } from "../../funcs/toolsUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateToolRequest$inboundSchema,
};

export const tool$toolsUpdate: ToolDefinition<typeof args> = {
  name: "tools-update",
  description: `Update tool

Updates a tool in the workspace.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await toolsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
