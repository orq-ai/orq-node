/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalsRagasCoherence } from "../../funcs/evalsRagasCoherence.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EvalsRagasCoherenceRequestBody$inboundSchema.optional(),
};

export const tool$evalsRagasCoherence: ToolDefinition<typeof args> = {
  name: "evals-ragas-coherence",
  description: `Run Coherence Evaluator`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evalsRagasCoherence(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
