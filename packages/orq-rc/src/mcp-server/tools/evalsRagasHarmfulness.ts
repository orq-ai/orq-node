/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalsRagasHarmfulness } from "../../funcs/evalsRagasHarmfulness.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EvalsRagasHarmfulnessRequestBody$inboundSchema.optional(),
};

export const tool$evalsRagasHarmfulness: ToolDefinition<typeof args> = {
  name: "evals-ragas-harmfulness",
  description: `Run Harmfulness Evaluator`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evalsRagasHarmfulness(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
