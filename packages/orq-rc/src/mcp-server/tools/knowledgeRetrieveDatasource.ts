/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { knowledgeRetrieveDatasource } from "../../funcs/knowledgeRetrieveDatasource.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RetrieveDatasourceRequest$inboundSchema,
};

export const tool$knowledgeRetrieveDatasource: ToolDefinition<typeof args> = {
  name: "knowledge-retrieve-datasource",
  description: `Retrieve a datasource`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await knowledgeRetrieveDatasource(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
