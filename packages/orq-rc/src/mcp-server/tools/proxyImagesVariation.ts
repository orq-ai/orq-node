/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { proxyImagesVariation } from "../../funcs/proxyImagesVariation.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostV2ProxyImagesVariationsRequestBody$inboundSchema,
};

export const tool$proxyImagesVariation: ToolDefinition<typeof args> = {
  name: "proxy-images-variation",
  description: `Create an Image Variation`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await proxyImagesVariation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
