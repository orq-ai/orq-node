/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { knowledgeCreateChunks } from "../../funcs/knowledgeCreateChunks.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateChunkRequest$inboundSchema,
};

export const tool$knowledgeCreateChunks: ToolDefinition<typeof args> = {
  name: "knowledge-create-chunks",
  description: `Create chunks for a datasource`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await knowledgeCreateChunks(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
