/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalsBotDetection } from "../../funcs/evalsBotDetection.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EvalsBotDetectionRequestBody$inboundSchema.optional(),
};

export const tool$evalsBotDetection: ToolDefinition<typeof args> = {
  name: "evals-bot-detection",
  description: `Run Bot Detection Evaluator`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evalsBotDetection(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
