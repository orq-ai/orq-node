/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalsLocalization } from "../../funcs/evalsLocalization.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EvalsLocalizationRequestBody$inboundSchema.optional(),
};

export const tool$evalsLocalization: ToolDefinition<typeof args> = {
  name: "evals-localization",
  description: `Run Localization Evaluator`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evalsLocalization(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
