/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsDelete } from "../../funcs/toolsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteToolRequest$inboundSchema,
};

export const tool$toolsDelete: ToolDefinition<typeof args> = {
  name: "tools-delete",
  description: `Delete tool

Deletes a tool by key.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await toolsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
