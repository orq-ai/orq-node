/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { proxyModerations } from "../../funcs/proxyModerations.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostV2ProxyModerationsRequestBody$inboundSchema,
};

export const tool$proxyModerations: ToolDefinition<typeof args> = {
  name: "proxy-moderations",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await proxyModerations(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
