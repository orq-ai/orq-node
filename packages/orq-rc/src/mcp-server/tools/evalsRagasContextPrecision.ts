/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalsRagasContextPrecision } from "../../funcs/evalsRagasContextPrecision.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EvalsRagasContextPrecisionRequestBody$inboundSchema
    .optional(),
};

export const tool$evalsRagasContextPrecision: ToolDefinition<typeof args> = {
  name: "evals-ragas-context-precision",
  description: `Run Context Precision Evaluator`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evalsRagasContextPrecision(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
