/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { proxyImagesGenerate } from "../../funcs/proxyImagesGenerate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostV2ProxyImagesGenerationsRequestBody$inboundSchema,
};

export const tool$proxyImagesGenerate: ToolDefinition<typeof args> = {
  name: "proxy-images-generate",
  description: `Create an Image`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await proxyImagesGenerate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
