/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsRetrieve } from "../../funcs/toolsRetrieve.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RetrieveToolRequest$inboundSchema,
};

export const tool$toolsRetrieve: ToolDefinition<typeof args> = {
  name: "tools-retrieve",
  description: `Retrieve tool

Retrieves a tool by key.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await toolsRetrieve(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
