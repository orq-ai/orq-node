/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sessionsUpdate } from "../../funcs/sessionsUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateSessionRequest$inboundSchema,
};

export const tool$sessionsUpdate: ToolDefinition<typeof args> = {
  name: "sessions-update",
  description: `Update Trace Session

Update a trace session`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sessionsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
