/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalsGrammar } from "../../funcs/evalsGrammar.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EvalsGrammarRequestBody$inboundSchema.optional(),
};

export const tool$evalsGrammar: ToolDefinition<typeof args> = {
  name: "evals-grammar",
  description: `Run Grammar Evaluator`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evalsGrammar(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
