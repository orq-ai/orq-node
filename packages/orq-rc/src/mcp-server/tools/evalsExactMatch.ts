/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalsExactMatch } from "../../funcs/evalsExactMatch.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EvalsExactMatchRequestBody$inboundSchema.optional(),
};

export const tool$evalsExactMatch: ToolDefinition<typeof args> = {
  name: "evals-exact-match",
  description: `Run Exact Match Evaluator`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evalsExactMatch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
