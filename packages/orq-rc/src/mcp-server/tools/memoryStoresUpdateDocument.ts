/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { memoryStoresUpdateDocument } from "../../funcs/memoryStoresUpdateDocument.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateMemoryDocumentRequest$inboundSchema,
};

export const tool$memoryStoresUpdateDocument: ToolDefinition<typeof args> = {
  name: "memory-stores-update-document",
  description: `Update a specific memory document

Updates the details of a specific memory document.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await memoryStoresUpdateDocument(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
