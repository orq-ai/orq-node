/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sessionsList } from "../../funcs/sessionsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListSessionsRequestBody$inboundSchema.optional(),
};

export const tool$sessionsList: ToolDefinition<typeof args> = {
  name: "sessions-list",
  description: `List sessions

Retrieves a paginated list of sessions for the current workspace.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sessionsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
