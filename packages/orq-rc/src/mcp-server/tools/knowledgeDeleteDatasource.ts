/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { knowledgeDeleteDatasource } from "../../funcs/knowledgeDeleteDatasource.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteDatasourceRequest$inboundSchema,
};

export const tool$knowledgeDeleteDatasource: ToolDefinition<typeof args> = {
  name: "knowledge-delete-datasource",
  description: `Deletes a datasource

Deletes a datasource from a knowledge base. Deleting a datasource will remove it from the knowledge base and all associated chunks. This action is irreversible and cannot be undone.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await knowledgeDeleteDatasource(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
