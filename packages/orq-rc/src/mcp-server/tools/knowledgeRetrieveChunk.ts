/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { knowledgeRetrieveChunk } from "../../funcs/knowledgeRetrieveChunk.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOneChunkRequest$inboundSchema,
};

export const tool$knowledgeRetrieveChunk: ToolDefinition<typeof args> = {
  name: "knowledge-retrieve-chunk",
  description: `Retrieve a chunk`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await knowledgeRetrieveChunk(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
