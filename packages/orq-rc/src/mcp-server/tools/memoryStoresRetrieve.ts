/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { memoryStoresRetrieve } from "../../funcs/memoryStoresRetrieve.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RetrieveMemoryStoreRequest$inboundSchema,
};

export const tool$memoryStoresRetrieve: ToolDefinition<typeof args> = {
  name: "memory-stores-retrieve",
  description: `Retrieve memory store

Retrieves detailed information about a specific memory store, including its configuration and metadata.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await memoryStoresRetrieve(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
