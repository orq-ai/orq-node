/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { deploymentsGetConfig } from "../../funcs/deploymentsGetConfig.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeploymentGetConfigRequestBody$inboundSchema,
};

export const tool$deploymentsGetConfig: ToolDefinition<typeof args> = {
  name: "deployments-get-config",
  description: `Get config

Retrieve the deployment configuration`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await deploymentsGetConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
