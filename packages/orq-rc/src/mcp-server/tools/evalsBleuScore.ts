/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalsBleuScore } from "../../funcs/evalsBleuScore.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EvalsBleuScoreRequestBody$inboundSchema.optional(),
};

export const tool$evalsBleuScore: ToolDefinition<typeof args> = {
  name: "evals-bleu-score",
  description: `Run BLEU Score Evaluator`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evalsBleuScore(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
