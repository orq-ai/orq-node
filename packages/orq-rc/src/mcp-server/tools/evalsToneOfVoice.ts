/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalsToneOfVoice } from "../../funcs/evalsToneOfVoice.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EvalsToneOfVoiceRequestBody$inboundSchema.optional(),
};

export const tool$evalsToneOfVoice: ToolDefinition<typeof args> = {
  name: "evals-tone-of-voice",
  description: `Run Tone of Voice Evaluator`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evalsToneOfVoice(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
