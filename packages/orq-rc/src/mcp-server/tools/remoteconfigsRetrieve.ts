/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { remoteconfigsRetrieve } from "../../funcs/remoteconfigsRetrieve.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoteConfigsGetConfigRequestBody$inboundSchema
    .optional(),
};

export const tool$remoteconfigsRetrieve: ToolDefinition<typeof args> = {
  name: "remoteconfigs-retrieve",
  description: `Retrieve a remote config`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await remoteconfigsRetrieve(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
