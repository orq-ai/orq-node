/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { knowledgeListChunks } from "../../funcs/knowledgeListChunks.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListChunksRequest$inboundSchema,
};

export const tool$knowledgeListChunks: ToolDefinition<typeof args> = {
  name: "knowledge-list-chunks",
  description: `List all chunks for a datasource`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await knowledgeListChunks(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
