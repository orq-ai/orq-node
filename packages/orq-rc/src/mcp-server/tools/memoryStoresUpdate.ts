/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { memoryStoresUpdate } from "../../funcs/memoryStoresUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateMemoryStoreRequest$inboundSchema,
};

export const tool$memoryStoresUpdate: ToolDefinition<typeof args> = {
  name: "memory-stores-update",
  description: `Update memory store

Update the memory store configuration`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await memoryStoresUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
