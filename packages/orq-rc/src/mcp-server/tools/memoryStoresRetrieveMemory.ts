/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { memoryStoresRetrieveMemory } from "../../funcs/memoryStoresRetrieveMemory.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RetrieveMemoryRequest$inboundSchema,
};

export const tool$memoryStoresRetrieveMemory: ToolDefinition<typeof args> = {
  name: "memory-stores-retrieve-memory",
  description: `Retrieve a specific memory

Retrieves details of a specific memory by its ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await memoryStoresRetrieveMemory(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
