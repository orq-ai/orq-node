/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { promptsRetrieve } from "../../funcs/promptsRetrieve.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOnePromptRequest$inboundSchema,
};

export const tool$promptsRetrieve: ToolDefinition<typeof args> = {
  name: "prompts_retrieve",
  description: `Retrieve a prompt

Retrieves a prompt object`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await promptsRetrieve(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
