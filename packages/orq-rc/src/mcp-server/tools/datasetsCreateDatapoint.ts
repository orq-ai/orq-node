/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { datasetsCreateDatapoint } from "../../funcs/datasetsCreateDatapoint.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateDatasetItemRequest$inboundSchema,
};

export const tool$datasetsCreateDatapoint: ToolDefinition<typeof args> = {
  name: "datasets_create-datapoint",
  description: `Create a datapoint

Creates a new datapoint in the specified dataset.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await datasetsCreateDatapoint(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
