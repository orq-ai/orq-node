/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { proxyResponsesCreate } from "../../funcs/proxyResponsesCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateResponseRequestBody$inboundSchema,
};

export const tool$proxyResponsesCreate: ToolDefinition<typeof args> = {
  name: "proxy-responses-create",
  description: `Create response

Creates a model response for the given input.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await proxyResponsesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
