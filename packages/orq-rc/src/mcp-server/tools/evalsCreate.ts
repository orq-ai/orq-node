/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalsCreate } from "../../funcs/evalsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateEvalRequestBody$inboundSchema.optional(),
};

export const tool$evalsCreate: ToolDefinition<typeof args> = {
  name: "evals-create",
  description: `Create an Evaluator`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evalsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
