/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sessionsCreate } from "../../funcs/sessionsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateSessionRequestBody$inboundSchema.optional(),
};

export const tool$sessionsCreate: ToolDefinition<typeof args> = {
  name: "sessions-create",
  description: `Create Trace Session

Create a session for traces`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sessionsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
