/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { memoryStoresRetrieveDocument } from "../../funcs/memoryStoresRetrieveDocument.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RetrieveMemoryDocumentRequest$inboundSchema,
};

export const tool$memoryStoresRetrieveDocument: ToolDefinition<typeof args> = {
  name: "memory-stores-retrieve-document",
  description: `Retrieve a specific memory document

Retrieves details of a specific memory document by its ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await memoryStoresRetrieveDocument(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
