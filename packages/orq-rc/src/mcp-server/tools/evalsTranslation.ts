/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalsTranslation } from "../../funcs/evalsTranslation.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EvalsTranslationRequestBody$inboundSchema.optional(),
};

export const tool$evalsTranslation: ToolDefinition<typeof args> = {
  name: "evals-translation",
  description: `Run Translation Evaluator`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evalsTranslation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
