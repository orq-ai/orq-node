/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sessionsDelete } from "../../funcs/sessionsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteSessionRequest$inboundSchema,
};

export const tool$sessionsDelete: ToolDefinition<typeof args> = {
  name: "sessions-delete",
  description: `Delete Trace Session

Delete a trace session`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sessionsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
