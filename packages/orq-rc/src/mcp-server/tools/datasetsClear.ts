/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { datasetsClear } from "../../funcs/datasetsClear.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ClearDatasetRequest$inboundSchema,
};

export const tool$datasetsClear: ToolDefinition<typeof args> = {
  name: "datasets-clear",
  description: `Delete all datapoints

Delete all datapoints from a dataset. This action is irreversible.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await datasetsClear(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
