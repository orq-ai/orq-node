/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { datasetsDeleteDatapoint } from "../../funcs/datasetsDeleteDatapoint.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteDatapointRequest$inboundSchema,
};

export const tool$datasetsDeleteDatapoint: ToolDefinition<typeof args> = {
  name: "datasets_delete-datapoint",
  description: `Delete a datapoint

Permanently deletes a specific datapoint from a dataset.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await datasetsDeleteDatapoint(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
