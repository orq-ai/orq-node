/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalsContainsUrl } from "../../funcs/evalsContainsUrl.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EvalsContainsUrlRequestBody$inboundSchema.optional(),
};

export const tool$evalsContainsUrl: ToolDefinition<typeof args> = {
  name: "evals-contains-url",
  description: `Run Contains URL Evaluator`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evalsContainsUrl(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
