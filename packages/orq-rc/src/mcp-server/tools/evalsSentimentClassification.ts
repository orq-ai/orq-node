/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evalsSentimentClassification } from "../../funcs/evalsSentimentClassification.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EvalsSentimentClassificationRequestBody$inboundSchema
    .optional(),
};

export const tool$evalsSentimentClassification: ToolDefinition<typeof args> = {
  name: "evals-sentiment-classification",
  description: `Run Sentiment Classification Evaluator`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evalsSentimentClassification(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
