/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { ClosedEnum } from "../../types/enums.js";

/**
 * Content type sent with webhook deliveries.
 */
export const CreateWebhookRequestContentType = {
  ApplicationJson: "application/json",
  ApplicationXWwwFormUrlencoded: "application/x-www-form-urlencoded",
} as const;
/**
 * Content type sent with webhook deliveries.
 */
export type CreateWebhookRequestContentType = ClosedEnum<
  typeof CreateWebhookRequestContentType
>;

/**
 * Configuration used to create a webhook in the current workspace.
 */
export type CreateWebhookRequest = {
  /**
   * Client-generated webhook ID.
   */
  id: string;
  /**
   * HTTPS endpoint that receives webhook deliveries.
   */
  url: string;
  /**
   * Content type sent with webhook deliveries.
   */
  contentType: CreateWebhookRequestContentType;
  /**
   * Human-readable webhook name.
   */
  displayName: string;
  /**
   * One or more workspace event slugs that trigger a delivery, for example `deployment.invoked` or `llm.response`.
   */
  events: Array<string>;
  /**
   * Signing secret returned by `GET /v2/webhooks/secret`.
   */
  secret: string;
  /**
   * Whether webhook deliveries are enabled.
   */
  enabled?: boolean | undefined;
};

/** @internal */
export const CreateWebhookRequestContentType$outboundSchema: z.ZodNativeEnum<
  typeof CreateWebhookRequestContentType
> = z.nativeEnum(CreateWebhookRequestContentType);

/** @internal */
export type CreateWebhookRequest$Outbound = {
  _id: string;
  url: string;
  content_type: string;
  display_name: string;
  events: Array<string>;
  secret: string;
  enabled?: boolean | undefined;
};

/** @internal */
export const CreateWebhookRequest$outboundSchema: z.ZodType<
  CreateWebhookRequest$Outbound,
  z.ZodTypeDef,
  CreateWebhookRequest
> = z.object({
  id: z.string(),
  url: z.string(),
  contentType: CreateWebhookRequestContentType$outboundSchema,
  displayName: z.string(),
  events: z.array(z.string()),
  secret: z.string(),
  enabled: z.boolean().optional(),
}).transform((v) => {
  return remap$(v, {
    id: "_id",
    contentType: "content_type",
    displayName: "display_name",
  });
});

export function createWebhookRequestToJSON(
  createWebhookRequest: CreateWebhookRequest,
): string {
  return JSON.stringify(
    CreateWebhookRequest$outboundSchema.parse(createWebhookRequest),
  );
}
