/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { MonitorWidget, MonitorWidget$inboundSchema } from "./monitorwidget.js";

/**
 * Preset kind.
 */
export const MonitorPresetKind = {
  Dashboard: "dashboard",
  Widget: "widget",
} as const;
/**
 * Preset kind.
 */
export type MonitorPresetKind = ClosedEnum<typeof MonitorPresetKind>;

export type MonitorPreset = {
  /**
   * Stable identifier for the preset, e.g. `genai-overview` or
   *
   * @remarks
   *  `widget-cost-by-model`.
   */
  presetKey: string;
  /**
   * Human-readable preset name shown in galleries.
   */
  displayName: string;
  /**
   * What the preset shows and when to use it.
   */
  description: string;
  /**
   * Preset kind.
   */
  kind: MonitorPresetKind;
  /**
   * Widgets the preset materializes into a create request. Widget
   *
   * @remarks
   *  presets carry exactly one entry.
   */
  widgets: Array<MonitorWidget>;
  /**
   * Default relative time range suggested by the preset.
   */
  defaultRange?: string | undefined;
};

/** @internal */
export const MonitorPresetKind$inboundSchema: z.ZodNativeEnum<
  typeof MonitorPresetKind
> = z.nativeEnum(MonitorPresetKind);

/** @internal */
export const MonitorPreset$inboundSchema: z.ZodType<
  MonitorPreset,
  z.ZodTypeDef,
  unknown
> = z.object({
  preset_key: z.string(),
  display_name: z.string(),
  description: z.string(),
  kind: MonitorPresetKind$inboundSchema,
  widgets: z.array(MonitorWidget$inboundSchema),
  default_range: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "preset_key": "presetKey",
    "display_name": "displayName",
    "default_range": "defaultRange",
  });
});

export function monitorPresetFromJSON(
  jsonString: string,
): SafeParseResult<MonitorPreset, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MonitorPreset$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MonitorPreset' from JSON`,
  );
}
