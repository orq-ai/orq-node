/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import {
  collectExtraKeys as collectExtraKeys$,
  safeParse,
} from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The event type. Matches the SSE `event` field.
 */
export const ResponseOutputItemStreamEventType = {
  ResponseOutputItemAdded: "response.output_item.added",
  ResponseOutputItemDone: "response.output_item.done",
} as const;
/**
 * The event type. Matches the SSE `event` field.
 */
export type ResponseOutputItemStreamEventType = ClosedEnum<
  typeof ResponseOutputItemStreamEventType
>;

/**
 * The event payload.
 */
export type ResponseOutputItemStreamEventData = {
  /**
   * The output item (message, function call, reasoning, etc.).
   */
  item: { [k: string]: any };
  /**
   * Index of the output item in the response output array.
   */
  outputIndex: number;
  /**
   * Monotonically increasing sequence number for ordering events.
   */
  sequenceNumber: number;
  /**
   * The event type. Matches the SSE `event` field.
   */
  type: ResponseOutputItemStreamEventType;
  additionalProperties?: { [k: string]: any } | undefined;
};

/**
 * The SSE event name, equal to the payload's `type`.
 */
export const ResponseOutputItemStreamEventEvent = {
  ResponseOutputItemAdded: "response.output_item.added",
  ResponseOutputItemDone: "response.output_item.done",
} as const;
/**
 * The SSE event name, equal to the payload's `type`.
 */
export type ResponseOutputItemStreamEventEvent = ClosedEnum<
  typeof ResponseOutputItemStreamEventEvent
>;

export type ResponseOutputItemStreamEvent = {
  /**
   * The event payload.
   */
  data: ResponseOutputItemStreamEventData;
  /**
   * The SSE event name, equal to the payload's `type`.
   */
  event: ResponseOutputItemStreamEventEvent;
};

/** @internal */
export const ResponseOutputItemStreamEventType$inboundSchema: z.ZodNativeEnum<
  typeof ResponseOutputItemStreamEventType
> = z.nativeEnum(ResponseOutputItemStreamEventType);

/** @internal */
export const ResponseOutputItemStreamEventData$inboundSchema: z.ZodType<
  ResponseOutputItemStreamEventData,
  z.ZodTypeDef,
  unknown
> = collectExtraKeys$(
  z.object({
    item: z.record(z.any()),
    output_index: z.number().int(),
    sequence_number: z.number().int(),
    type: ResponseOutputItemStreamEventType$inboundSchema,
  }).catchall(z.any()),
  "additionalProperties",
  true,
).transform((v) => {
  return remap$(v, {
    "output_index": "outputIndex",
    "sequence_number": "sequenceNumber",
  });
});

export function responseOutputItemStreamEventDataFromJSON(
  jsonString: string,
): SafeParseResult<ResponseOutputItemStreamEventData, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ResponseOutputItemStreamEventData$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ResponseOutputItemStreamEventData' from JSON`,
  );
}

/** @internal */
export const ResponseOutputItemStreamEventEvent$inboundSchema: z.ZodNativeEnum<
  typeof ResponseOutputItemStreamEventEvent
> = z.nativeEnum(ResponseOutputItemStreamEventEvent);

/** @internal */
export const ResponseOutputItemStreamEvent$inboundSchema: z.ZodType<
  ResponseOutputItemStreamEvent,
  z.ZodTypeDef,
  unknown
> = z.object({
  data: z.unknown().transform((v, ctx) => {
    if (typeof v !== "string") return v;
    try {
      return JSON.parse(v);
    } catch (err) {
      ctx.addIssue({ code: "custom", message: `malformed json: ${err}` });
      return z.NEVER;
    }
  }).pipe(z.lazy(() => ResponseOutputItemStreamEventData$inboundSchema)),
  event: ResponseOutputItemStreamEventEvent$inboundSchema,
});

export function responseOutputItemStreamEventFromJSON(
  jsonString: string,
): SafeParseResult<ResponseOutputItemStreamEvent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ResponseOutputItemStreamEvent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ResponseOutputItemStreamEvent' from JSON`,
  );
}
