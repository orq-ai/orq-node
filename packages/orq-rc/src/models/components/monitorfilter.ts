/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Predicate operator. `eq` and `neq` accept exactly one value; `in` and `not_in` accept 1-100 values.
 */
export const Op = {
  Eq: "eq",
  Neq: "neq",
  In: "in",
  NotIn: "not_in",
  Gt: "gt",
  Gte: "gte",
  Lt: "lt",
  Lte: "lte",
  Between: "between",
  Contains: "contains",
  Exists: "exists",
  NotExists: "not_exists",
} as const;
/**
 * Predicate operator. `eq` and `neq` accept exactly one value; `in` and `not_in` accept 1-100 values.
 */
export type Op = ClosedEnum<typeof Op>;

export type MonitorFilter = {
  /**
   * Reporting dimension to filter on. Valid fields depend on the
   *
   * @remarks
   *  metric; validated when the monitor is saved.
   */
  field: string;
  /**
   * Predicate operator. `eq` and `neq` accept exactly one value; `in` and `not_in` accept 1-100 values.
   */
  op: Op;
  /**
   * Values compared against the selected field.
   */
  values: Array<string>;
};

/** @internal */
export const Op$inboundSchema: z.ZodNativeEnum<typeof Op> = z.nativeEnum(Op);
/** @internal */
export const Op$outboundSchema: z.ZodNativeEnum<typeof Op> = Op$inboundSchema;

/** @internal */
export const MonitorFilter$inboundSchema: z.ZodType<
  MonitorFilter,
  z.ZodTypeDef,
  unknown
> = z.object({
  field: z.string(),
  op: Op$inboundSchema,
  values: z.array(z.string()),
});
/** @internal */
export type MonitorFilter$Outbound = {
  field: string;
  op: string;
  values: Array<string>;
};

/** @internal */
export const MonitorFilter$outboundSchema: z.ZodType<
  MonitorFilter$Outbound,
  z.ZodTypeDef,
  MonitorFilter
> = z.object({
  field: z.string(),
  op: Op$outboundSchema,
  values: z.array(z.string()),
});

export function monitorFilterToJSON(monitorFilter: MonitorFilter): string {
  return JSON.stringify(MonitorFilter$outboundSchema.parse(monitorFilter));
}
export function monitorFilterFromJSON(
  jsonString: string,
): SafeParseResult<MonitorFilter, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MonitorFilter$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MonitorFilter' from JSON`,
  );
}
