/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * BudgetMatch carries the CEL expression that decides whether a budget
 *
 * @remarks
 *  applies to a request. Available variables: `model`, `provider`,
 *  `model_id`, `api_key`, `identity`, `project`, `metadata` (map),
 *  `headers` (map, lowercase keys). An empty expression always matches.
 *  Expressions are syntax-validated at write time.
 */
export type BudgetMatch = {
  cel?: string | undefined;
};

/** @internal */
export const BudgetMatch$inboundSchema: z.ZodType<
  BudgetMatch,
  z.ZodTypeDef,
  unknown
> = z.object({
  cel: z.string().optional(),
});
/** @internal */
export type BudgetMatch$Outbound = {
  cel?: string | undefined;
};

/** @internal */
export const BudgetMatch$outboundSchema: z.ZodType<
  BudgetMatch$Outbound,
  z.ZodTypeDef,
  BudgetMatch
> = z.object({
  cel: z.string().optional(),
});

export function budgetMatchToJSON(budgetMatch: BudgetMatch): string {
  return JSON.stringify(BudgetMatch$outboundSchema.parse(budgetMatch));
}
export function budgetMatchFromJSON(
  jsonString: string,
): SafeParseResult<BudgetMatch, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BudgetMatch$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BudgetMatch' from JSON`,
  );
}
