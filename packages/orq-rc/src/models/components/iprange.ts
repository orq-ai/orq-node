/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type IPRange = {
  id: string;
  cidr: string;
  description: string;
  createdAt: Date;
  createdById?: string | undefined;
};

/** @internal */
export const IPRange$inboundSchema: z.ZodType<IPRange, z.ZodTypeDef, unknown> =
  z.object({
    id: z.string(),
    cidr: z.string(),
    description: z.string(),
    created_at: z.string().datetime({ offset: true }).transform(v =>
      new Date(v)
    ),
    created_by_id: z.string().optional(),
  }).transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "created_by_id": "createdById",
    });
  });

export function ipRangeFromJSON(
  jsonString: string,
): SafeParseResult<IPRange, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => IPRange$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'IPRange' from JSON`,
  );
}
