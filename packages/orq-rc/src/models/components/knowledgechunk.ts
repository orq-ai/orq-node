/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { CountMetadata, CountMetadata$inboundSchema } from "./countmetadata.js";
import {
  ProcessingAttempt,
  ProcessingAttempt$inboundSchema,
} from "./processingattempt.js";

export type Metadata = string | number | boolean;

/**
 * The status of the chunk
 */
export const KnowledgeChunkStatus = {
  Pending: "pending",
  Processing: "processing",
  Completed: "completed",
  Failed: "failed",
  Queued: "queued",
} as const;
/**
 * The status of the chunk
 */
export type KnowledgeChunkStatus = ClosedEnum<typeof KnowledgeChunkStatus>;

export type KnowledgeChunk = {
  text: string;
  /**
   * Metadata of the chunk. Can include `page_number` or any other key-value pairs
   */
  metadata?: { [k: string]: string | number | boolean } | undefined;
  enabled: boolean;
  /**
   * The status of the chunk
   */
  status: KnowledgeChunkStatus;
  created: string;
  updated: string;
  /**
   * The unique identifier of the user who created the chunk
   */
  createdById?: string | null | undefined;
  /**
   * The unique identifier of the user who updated the chunk
   */
  updateById?: string | null | undefined;
  /**
   * Additive compatibility fields from the database document.
   */
  processingAttempts?: Array<ProcessingAttempt> | undefined;
  countMetadata?: CountMetadata | undefined;
  id: string;
};

/** @internal */
export const Metadata$inboundSchema: z.ZodType<
  Metadata,
  z.ZodTypeDef,
  unknown
> = z.union([z.string(), z.number(), z.boolean()]);

export function metadataFromJSON(
  jsonString: string,
): SafeParseResult<Metadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Metadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Metadata' from JSON`,
  );
}

/** @internal */
export const KnowledgeChunkStatus$inboundSchema: z.ZodNativeEnum<
  typeof KnowledgeChunkStatus
> = z.nativeEnum(KnowledgeChunkStatus);

/** @internal */
export const KnowledgeChunk$inboundSchema: z.ZodType<
  KnowledgeChunk,
  z.ZodTypeDef,
  unknown
> = z.object({
  text: z.string(),
  metadata: z.record(z.union([z.string(), z.number(), z.boolean()])).optional(),
  enabled: z.boolean(),
  status: KnowledgeChunkStatus$inboundSchema,
  created: z.string(),
  updated: z.string(),
  created_by_id: z.nullable(z.string()).optional(),
  update_by_id: z.nullable(z.string()).optional(),
  processing_attempts: z.array(ProcessingAttempt$inboundSchema).optional(),
  count_metadata: CountMetadata$inboundSchema.optional(),
  _id: z.string(),
}).transform((v) => {
  return remap$(v, {
    "created_by_id": "createdById",
    "update_by_id": "updateById",
    "processing_attempts": "processingAttempts",
    "count_metadata": "countMetadata",
    "_id": "id",
  });
});

export function knowledgeChunkFromJSON(
  jsonString: string,
): SafeParseResult<KnowledgeChunk, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => KnowledgeChunk$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'KnowledgeChunk' from JSON`,
  );
}
