/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  SmartRouterProfile,
  SmartRouterProfile$inboundSchema,
} from "./smartrouterprofile.js";

export type SmartRouter = {
  /**
   * Unique Smart Router identifier assigned by ORQ.
   */
  smartRouterId: string;
  /**
   * Stable lowercase key used in the gateway model reference.
   */
  key: string;
  /**
   * Workspace-qualified gateway model reference.
   */
  modelRef: string;
  /**
   * Ordered model pool in provider/model format, from highest to lowest capability.
   */
  models: Array<string>;
  profile: SmartRouterProfile;
  /**
   * Whether the router is available to gateway requests.
   */
  enabled: boolean;
  createdAt: Date;
  updatedAt: Date;
};

/** @internal */
export const SmartRouter$inboundSchema: z.ZodType<
  SmartRouter,
  z.ZodTypeDef,
  unknown
> = z.object({
  smart_router_id: z.string(),
  key: z.string(),
  model_ref: z.string(),
  models: z.array(z.string()),
  profile: SmartRouterProfile$inboundSchema,
  enabled: z.boolean(),
  created_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  updated_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
}).transform((v) => {
  return remap$(v, {
    "smart_router_id": "smartRouterId",
    "model_ref": "modelRef",
    "created_at": "createdAt",
    "updated_at": "updatedAt",
  });
});

export function smartRouterFromJSON(
  jsonString: string,
): SafeParseResult<SmartRouter, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SmartRouter$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SmartRouter' from JSON`,
  );
}
