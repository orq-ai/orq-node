/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type MemoryDocument = {
  memoryId: string;
  storeId: string;
  /**
   * The content of the memory document (whitespace trimmed).
   */
  text: string;
  created: string;
  updated: string;
  createdById?: string | null | undefined;
  updatedById?: string | null | undefined;
  workspaceId: string;
  /**
   * Flexible key-value pairs for custom filtering and categorization. Clients can add arbitrary string metadata to enable future filtering of memory documents based on their specific needs (e.g., document type, source, topic, relevance score, or any custom taxonomy).
   */
  metadata?: { [k: string]: string } | undefined;
  id: string;
};

/** @internal */
export const MemoryDocument$inboundSchema: z.ZodType<
  MemoryDocument,
  z.ZodTypeDef,
  unknown
> = z.object({
  memory_id: z.string(),
  store_id: z.string(),
  text: z.string(),
  created: z.string(),
  updated: z.string(),
  created_by_id: z.nullable(z.string()).optional(),
  updated_by_id: z.nullable(z.string()).optional(),
  workspace_id: z.string(),
  metadata: z.record(z.string()).optional(),
  _id: z.string(),
}).transform((v) => {
  return remap$(v, {
    "memory_id": "memoryId",
    "store_id": "storeId",
    "created_by_id": "createdById",
    "updated_by_id": "updatedById",
    "workspace_id": "workspaceId",
    "_id": "id",
  });
});

export function memoryDocumentFromJSON(
  jsonString: string,
): SafeParseResult<MemoryDocument, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MemoryDocument$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MemoryDocument' from JSON`,
  );
}
