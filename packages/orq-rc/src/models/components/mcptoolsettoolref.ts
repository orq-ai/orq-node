/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * McpToolsetToolRef points at a single tool by its origin server and the
 *
 * @remarks
 *  tool's bare (upstream) name.
 */
export type McpToolsetToolRef = {
  mcpServerId?: string | undefined;
  toolName?: string | undefined;
};

/** @internal */
export const McpToolsetToolRef$inboundSchema: z.ZodType<
  McpToolsetToolRef,
  z.ZodTypeDef,
  unknown
> = z.object({
  mcp_server_id: z.string().optional(),
  tool_name: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "mcp_server_id": "mcpServerId",
    "tool_name": "toolName",
  });
});
/** @internal */
export type McpToolsetToolRef$Outbound = {
  mcp_server_id?: string | undefined;
  tool_name?: string | undefined;
};

/** @internal */
export const McpToolsetToolRef$outboundSchema: z.ZodType<
  McpToolsetToolRef$Outbound,
  z.ZodTypeDef,
  McpToolsetToolRef
> = z.object({
  mcpServerId: z.string().optional(),
  toolName: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    mcpServerId: "mcp_server_id",
    toolName: "tool_name",
  });
});

export function mcpToolsetToolRefToJSON(
  mcpToolsetToolRef: McpToolsetToolRef,
): string {
  return JSON.stringify(
    McpToolsetToolRef$outboundSchema.parse(mcpToolsetToolRef),
  );
}
export function mcpToolsetToolRefFromJSON(
  jsonString: string,
): SafeParseResult<McpToolsetToolRef, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => McpToolsetToolRef$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'McpToolsetToolRef' from JSON`,
  );
}
