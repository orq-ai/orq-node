/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type McpOAuthConfigOutput = {
  /**
   * OAuth client identifier presented to the upstream authorization server.
   */
  clientId?: string | undefined;
  /**
   * Token endpoint the gateway calls to mint upstream access tokens.
   */
  tokenUrl?: string | undefined;
  /**
   * Scopes requested when minting upstream access tokens.
   */
  scopes?: Array<string> | undefined;
  /**
   * Redacted preview of `client_secret`, returned in its place.
   */
  maskedValue?: string | undefined;
};

/** @internal */
export const McpOAuthConfigOutput$inboundSchema: z.ZodType<
  McpOAuthConfigOutput,
  z.ZodTypeDef,
  unknown
> = z.object({
  client_id: z.string().optional(),
  token_url: z.string().optional(),
  scopes: z.array(z.string()).optional(),
  masked_value: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "client_id": "clientId",
    "token_url": "tokenUrl",
    "masked_value": "maskedValue",
  });
});

export function mcpOAuthConfigOutputFromJSON(
  jsonString: string,
): SafeParseResult<McpOAuthConfigOutput, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => McpOAuthConfigOutput$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'McpOAuthConfigOutput' from JSON`,
  );
}
