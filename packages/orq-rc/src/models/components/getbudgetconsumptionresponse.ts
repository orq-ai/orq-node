/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { BudgetPeriod, BudgetPeriod$inboundSchema } from "./budgetperiod.js";

export type GetBudgetConsumptionResponse = {
  /**
   * Budget id whose counters are reported.
   */
  budgetId?: string | undefined;
  period?: BudgetPeriod | undefined;
  /**
   * Period bucket label (e.g. "2025-05-27" for DAILY, "2025-05" for
   *
   * @remarks
   *  MONTHLY). Empty when no counters exist for the current period.
   */
  periodDate?: string | undefined;
  /**
   * Cost accumulated in the current period, in USD.
   */
  cost?: number | undefined;
  /**
   * Tokens accumulated in the current period. Carried as a double (not
   *
   * @remarks
   *  int64) so it serializes as a JSON number rather than a quoted
   *  string, matching BudgetUsage.tokens and limits.token_limit.
   */
  tokens?: number | undefined;
  /**
   * Requests counted in the current 60-second rolling window.
   */
  requestsInWindow?: number | undefined;
};

/** @internal */
export const GetBudgetConsumptionResponse$inboundSchema: z.ZodType<
  GetBudgetConsumptionResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  budget_id: z.string().optional(),
  period: BudgetPeriod$inboundSchema.optional(),
  period_date: z.string().optional(),
  cost: z.number().optional(),
  tokens: z.number().optional(),
  requests_in_window: z.number().int().optional(),
}).transform((v) => {
  return remap$(v, {
    "budget_id": "budgetId",
    "period_date": "periodDate",
    "requests_in_window": "requestsInWindow",
  });
});

export function getBudgetConsumptionResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetBudgetConsumptionResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetBudgetConsumptionResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetBudgetConsumptionResponse' from JSON`,
  );
}
