/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import {
  collectExtraKeys as collectExtraKeys$,
  safeParse,
} from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  PublicResponseResource,
  PublicResponseResource$inboundSchema,
} from "./publicresponseresource.js";

/**
 * A `response.queued` server-sent event.
 */
export type ResponseQueuedStreamEvent = {
  response: PublicResponseResource;
  /**
   * Monotonically increasing sequence number for ordering events.
   */
  sequenceNumber: number;
  /**
   * The event type. Discriminates the payload.
   */
  type: "response.queued";
  additionalProperties?: { [k: string]: any } | undefined;
};

/** @internal */
export const ResponseQueuedStreamEvent$inboundSchema: z.ZodType<
  ResponseQueuedStreamEvent,
  z.ZodTypeDef,
  unknown
> = collectExtraKeys$(
  z.object({
    response: PublicResponseResource$inboundSchema,
    sequence_number: z.number().int(),
    type: z.literal("response.queued"),
  }).catchall(z.any()),
  "additionalProperties",
  true,
).transform((v) => {
  return remap$(v, {
    "sequence_number": "sequenceNumber",
  });
});

export function responseQueuedStreamEventFromJSON(
  jsonString: string,
): SafeParseResult<ResponseQueuedStreamEvent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ResponseQueuedStreamEvent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ResponseQueuedStreamEvent' from JSON`,
  );
}
