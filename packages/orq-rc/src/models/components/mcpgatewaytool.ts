/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  McpToolAnnotations,
  McpToolAnnotations$inboundSchema,
} from "./mcptoolannotations.js";

/**
 * JSON Schema for the tool arguments, as advertised upstream.
 */
export type InputSchema = {};

export type McpGatewayTool = {
  /**
   * Key of the upstream server the tool comes from.
   */
  serverKey?: string | undefined;
  /**
   * Alias configured on the gateway link the tool comes from.
   */
  alias?: string | undefined;
  /**
   * Name MCP clients call; namespaced per the gateway's `tool_naming` and capped at 64 characters.
   */
  exposedName?: string | undefined;
  /**
   * Tool description advertised upstream, passed through to MCP clients.
   */
  description?: string | undefined;
  /**
   * JSON Schema for the tool arguments, as advertised upstream.
   */
  inputSchema?: InputSchema | undefined;
  /**
   * Upstream server the exposed tool resolves to.
   */
  mcpServerId?: string | undefined;
  /**
   * Bare tool name on the upstream server, before namespacing.
   */
  upstreamToolName?: string | undefined;
  /**
   * Human-readable label advertised upstream, when the server sends one.
   */
  title?: string | undefined;
  /**
   * Behaviour hints claimed by the upstream server.
   */
  annotations?: McpToolAnnotations | undefined;
};

/** @internal */
export const InputSchema$inboundSchema: z.ZodType<
  InputSchema,
  z.ZodTypeDef,
  unknown
> = z.object({});

export function inputSchemaFromJSON(
  jsonString: string,
): SafeParseResult<InputSchema, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InputSchema$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InputSchema' from JSON`,
  );
}

/** @internal */
export const McpGatewayTool$inboundSchema: z.ZodType<
  McpGatewayTool,
  z.ZodTypeDef,
  unknown
> = z.object({
  server_key: z.string().optional(),
  alias: z.string().optional(),
  exposed_name: z.string().optional(),
  description: z.string().optional(),
  input_schema: z.lazy(() => InputSchema$inboundSchema).optional(),
  mcp_server_id: z.string().optional(),
  upstream_tool_name: z.string().optional(),
  title: z.string().optional(),
  annotations: McpToolAnnotations$inboundSchema.optional(),
}).transform((v) => {
  return remap$(v, {
    "server_key": "serverKey",
    "exposed_name": "exposedName",
    "input_schema": "inputSchema",
    "mcp_server_id": "mcpServerId",
    "upstream_tool_name": "upstreamToolName",
  });
});

export function mcpGatewayToolFromJSON(
  jsonString: string,
): SafeParseResult<McpGatewayTool, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => McpGatewayTool$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'McpGatewayTool' from JSON`,
  );
}
