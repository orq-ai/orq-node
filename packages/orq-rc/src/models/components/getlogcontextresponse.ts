/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { Log, Log$inboundSchema } from "./log.js";

export type GetLogContextResponse = {
  /**
   * Log is the canonical wire representation of an otel_logs row.
   */
  anchor: Log;
  /**
   * Matching records earlier than the anchor, ordered oldest to newest.
   */
  before: Array<Log>;
  /**
   * Matching records later than the anchor, ordered oldest to newest.
   */
  after: Array<Log>;
  hasMoreBefore: boolean;
  hasMoreAfter: boolean;
};

/** @internal */
export const GetLogContextResponse$inboundSchema: z.ZodType<
  GetLogContextResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  anchor: Log$inboundSchema,
  before: z.array(Log$inboundSchema),
  after: z.array(Log$inboundSchema),
  has_more_before: z.boolean(),
  has_more_after: z.boolean(),
}).transform((v) => {
  return remap$(v, {
    "has_more_before": "hasMoreBefore",
    "has_more_after": "hasMoreAfter",
  });
});

export function getLogContextResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetLogContextResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetLogContextResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetLogContextResponse' from JSON`,
  );
}
