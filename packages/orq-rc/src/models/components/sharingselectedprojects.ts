/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type SharingSelectedProjects = {
  /**
   * Projects allowed to use the entity. An empty list shares it with no project.
   */
  projectIds?: Array<string> | undefined;
};

/** @internal */
export const SharingSelectedProjects$inboundSchema: z.ZodType<
  SharingSelectedProjects,
  z.ZodTypeDef,
  unknown
> = z.object({
  project_ids: z.array(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    "project_ids": "projectIds",
  });
});
/** @internal */
export type SharingSelectedProjects$Outbound = {
  project_ids?: Array<string> | undefined;
};

/** @internal */
export const SharingSelectedProjects$outboundSchema: z.ZodType<
  SharingSelectedProjects$Outbound,
  z.ZodTypeDef,
  SharingSelectedProjects
> = z.object({
  projectIds: z.array(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    projectIds: "project_ids",
  });
});

export function sharingSelectedProjectsToJSON(
  sharingSelectedProjects: SharingSelectedProjects,
): string {
  return JSON.stringify(
    SharingSelectedProjects$outboundSchema.parse(sharingSelectedProjects),
  );
}
export function sharingSelectedProjectsFromJSON(
  jsonString: string,
): SafeParseResult<SharingSelectedProjects, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SharingSelectedProjects$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SharingSelectedProjects' from JSON`,
  );
}
