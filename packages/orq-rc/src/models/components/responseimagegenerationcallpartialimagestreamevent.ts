/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import {
  collectExtraKeys as collectExtraKeys$,
  safeParse,
} from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A `response.image_generation_call.partial_image` server-sent event.
 */
export type ResponseImageGenerationCallPartialImageStreamEvent = {
  /**
   * ID of the output item this event refers to.
   */
  itemId: string;
  /**
   * Index of the output item in the response output array.
   */
  outputIndex: number;
  /**
   * Base64-encoded partial image.
   */
  partialImageB64: string;
  /**
   * Index of the partial image.
   */
  partialImageIndex: number;
  /**
   * Monotonically increasing sequence number for ordering events.
   */
  sequenceNumber: number;
  /**
   * The event type. Discriminates the payload.
   */
  type: "response.image_generation_call.partial_image";
  additionalProperties?: { [k: string]: any } | undefined;
};

/** @internal */
export const ResponseImageGenerationCallPartialImageStreamEvent$inboundSchema:
  z.ZodType<
    ResponseImageGenerationCallPartialImageStreamEvent,
    z.ZodTypeDef,
    unknown
  > = collectExtraKeys$(
    z.object({
      item_id: z.string(),
      output_index: z.number().int(),
      partial_image_b64: z.string(),
      partial_image_index: z.number().int(),
      sequence_number: z.number().int(),
      type: z.literal("response.image_generation_call.partial_image"),
    }).catchall(z.any()),
    "additionalProperties",
    true,
  ).transform((v) => {
    return remap$(v, {
      "item_id": "itemId",
      "output_index": "outputIndex",
      "partial_image_b64": "partialImageB64",
      "partial_image_index": "partialImageIndex",
      "sequence_number": "sequenceNumber",
    });
  });

export function responseImageGenerationCallPartialImageStreamEventFromJSON(
  jsonString: string,
): SafeParseResult<
  ResponseImageGenerationCallPartialImageStreamEvent,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      ResponseImageGenerationCallPartialImageStreamEvent$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'ResponseImageGenerationCallPartialImageStreamEvent' from JSON`,
  );
}
