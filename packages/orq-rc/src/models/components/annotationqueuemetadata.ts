/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Aggregate counters maintained by the service as items are added and removed.
 */
export type AnnotationQueueMetadata = {
  /**
   * Number of items currently in the annotation queue.
   */
  itemsCount: number;
};

/** @internal */
export const AnnotationQueueMetadata$inboundSchema: z.ZodType<
  AnnotationQueueMetadata,
  z.ZodTypeDef,
  unknown
> = z.object({
  items_count: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    "items_count": "itemsCount",
  });
});

export function annotationQueueMetadataFromJSON(
  jsonString: string,
): SafeParseResult<AnnotationQueueMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AnnotationQueueMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AnnotationQueueMetadata' from JSON`,
  );
}
