/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  AnnotationQueue,
  AnnotationQueue$inboundSchema,
} from "./annotationqueue.js";

export type ListAnnotationQueuesResponse = {
  /**
   * Object discriminator for list responses; always `list`.
   */
  object: string;
  /**
   * Page of annotation queues.
   */
  data: Array<AnnotationQueue>;
  /**
   * Whether more annotation queues are available in the selected pagination direction.
   */
  hasMore: boolean;
};

/** @internal */
export const ListAnnotationQueuesResponse$inboundSchema: z.ZodType<
  ListAnnotationQueuesResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  object: z.string(),
  data: z.array(AnnotationQueue$inboundSchema),
  has_more: z.boolean(),
}).transform((v) => {
  return remap$(v, {
    "has_more": "hasMore",
  });
});

export function listAnnotationQueuesResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListAnnotationQueuesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListAnnotationQueuesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListAnnotationQueuesResponse' from JSON`,
  );
}
