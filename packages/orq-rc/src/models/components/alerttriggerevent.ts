/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { AlertEvidence, AlertEvidence$inboundSchema } from "./alertevidence.js";

export type AlertTriggerEvent = {
  /**
   * Unique event identifier, for example `alertevent_01H...`.
   */
  eventId: string;
  /**
   * Trigger the event was recorded on.
   */
  triggerId: string;
  /**
   * Alert the event belongs to.
   */
  alertId: string;
  /**
   * Time of the evaluation tick.
   */
  at: Date;
  /**
   * Observed metric value at the tick.
   */
  value: number;
  /**
   * Exemplar traces that contributed to the breach, worst first.
   */
  evidence?: Array<AlertEvidence> | undefined;
};

/** @internal */
export const AlertTriggerEvent$inboundSchema: z.ZodType<
  AlertTriggerEvent,
  z.ZodTypeDef,
  unknown
> = z.object({
  event_id: z.string(),
  trigger_id: z.string(),
  alert_id: z.string(),
  at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  value: z.number(),
  evidence: z.array(AlertEvidence$inboundSchema).optional(),
}).transform((v) => {
  return remap$(v, {
    "event_id": "eventId",
    "trigger_id": "triggerId",
    "alert_id": "alertId",
  });
});

export function alertTriggerEventFromJSON(
  jsonString: string,
): SafeParseResult<AlertTriggerEvent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AlertTriggerEvent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AlertTriggerEvent' from JSON`,
  );
}
