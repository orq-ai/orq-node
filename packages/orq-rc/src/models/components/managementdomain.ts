/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  ManagementDomainGroup,
  ManagementDomainGroup$inboundSchema,
} from "./managementdomaingroup.js";

/**
 * ManagementDomain describes a permission domain that can be granted
 *
 * @remarks
 *  to a management key. Verbs are derived from id + readable/writable.
 */
export type ManagementDomain = {
  /**
   * Stable domain identifier (e.g. "api-key", "budget"). Used as the
   *
   * @remarks
   *  key in ManagementKey.access and as the verb prefix in resolved
   *  permissions (e.g. budget.list, budget.view, budget.create).
   */
  id?: string | undefined;
  /**
   * Human-readable label for the dashboard.
   */
  displayName?: string | undefined;
  group?: ManagementDomainGroup | undefined;
  /**
   * Whether this domain can be granted read access.
   */
  readable?: boolean | undefined;
  /**
   * Whether this domain can be granted write access.
   */
  writable?: boolean | undefined;
};

/** @internal */
export const ManagementDomain$inboundSchema: z.ZodType<
  ManagementDomain,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string().optional(),
  display_name: z.string().optional(),
  group: ManagementDomainGroup$inboundSchema.optional(),
  readable: z.boolean().optional(),
  writable: z.boolean().optional(),
}).transform((v) => {
  return remap$(v, {
    "display_name": "displayName",
  });
});

export function managementDomainFromJSON(
  jsonString: string,
): SafeParseResult<ManagementDomain, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ManagementDomain$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ManagementDomain' from JSON`,
  );
}
