/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  DomainVerificationStatus,
  DomainVerificationStatus$inboundSchema,
} from "./domainverificationstatus.js";

export type DomainVerification = {
  id: string;
  domain: string;
  status: DomainVerificationStatus;
  txtName: string;
  txtValue: string;
  createdAt: Date;
  expiresAt?: Date | undefined;
  verifiedAt?: Date | undefined;
  createdById?: string | undefined;
};

/** @internal */
export const DomainVerification$inboundSchema: z.ZodType<
  DomainVerification,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  domain: z.string(),
  status: DomainVerificationStatus$inboundSchema,
  txt_name: z.string(),
  txt_value: z.string(),
  created_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  expires_at: z.string().datetime({ offset: true }).transform(v => new Date(v))
    .optional(),
  verified_at: z.string().datetime({ offset: true }).transform(v => new Date(v))
    .optional(),
  created_by_id: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "txt_name": "txtName",
    "txt_value": "txtValue",
    "created_at": "createdAt",
    "expires_at": "expiresAt",
    "verified_at": "verifiedAt",
    "created_by_id": "createdById",
  });
});

export function domainVerificationFromJSON(
  jsonString: string,
): SafeParseResult<DomainVerification, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DomainVerification$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DomainVerification' from JSON`,
  );
}
