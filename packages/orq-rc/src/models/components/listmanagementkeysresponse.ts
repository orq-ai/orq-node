/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { ManagementKey, ManagementKey$inboundSchema } from "./managementkey.js";

export type ListManagementKeysResponse = {
  /**
   * Object discriminator for list responses; always `list`.
   */
  object: string;
  /**
   * Page of management-keys, ordered newest first. `token_hash` and
   *
   * @remarks
   *  `api_key` are always elided in list responses.
   */
  data: Array<ManagementKey>;
  /**
   * Whether more management-keys are available in the selected pagination
   *
   * @remarks
   *  direction.
   */
  hasMore: boolean;
};

/** @internal */
export const ListManagementKeysResponse$inboundSchema: z.ZodType<
  ListManagementKeysResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  object: z.string(),
  data: z.array(ManagementKey$inboundSchema),
  has_more: z.boolean(),
}).transform((v) => {
  return remap$(v, {
    "has_more": "hasMore",
  });
});

export function listManagementKeysResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListManagementKeysResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListManagementKeysResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListManagementKeysResponse' from JSON`,
  );
}
