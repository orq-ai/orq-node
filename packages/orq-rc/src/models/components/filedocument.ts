/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type FileDocument = {
  id: string;
  bytes: number;
  created: string;
  fileName: string;
  purpose: string;
  workspaceId: string;
};

/** @internal */
export const FileDocument$inboundSchema: z.ZodType<
  FileDocument,
  z.ZodTypeDef,
  unknown
> = z.object({
  _id: z.string(),
  bytes: z.number().int(),
  created: z.string(),
  file_name: z.string(),
  purpose: z.string(),
  workspace_id: z.string(),
}).transform((v) => {
  return remap$(v, {
    "_id": "id",
    "file_name": "fileName",
    "workspace_id": "workspaceId",
  });
});

export function fileDocumentFromJSON(
  jsonString: string,
): SafeParseResult<FileDocument, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FileDocument$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FileDocument' from JSON`,
  );
}
