/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Always "model".
 */
export const ObjectT = {
  Model: "model",
} as const;
/**
 * Always "model".
 */
export type ObjectT = ClosedEnum<typeof ObjectT>;

export type PublicModelEntry = {
  /**
   * Unix timestamp (seconds) when the model was added.
   */
  created: number;
  /**
   * Model identifier in provider/model format (e.g. openai/gpt-4o).
   */
  id: string;
  /**
   * Always "model".
   */
  object: ObjectT;
  /**
   * The provider that owns the model (e.g. openai, anthropic).
   */
  ownedBy: string;
};

/** @internal */
export const ObjectT$inboundSchema: z.ZodNativeEnum<typeof ObjectT> = z
  .nativeEnum(ObjectT);

/** @internal */
export const PublicModelEntry$inboundSchema: z.ZodType<
  PublicModelEntry,
  z.ZodTypeDef,
  unknown
> = z.object({
  created: z.number().int(),
  id: z.string(),
  object: ObjectT$inboundSchema,
  owned_by: z.string(),
}).transform((v) => {
  return remap$(v, {
    "owned_by": "ownedBy",
  });
});

export function publicModelEntryFromJSON(
  jsonString: string,
): SafeParseResult<PublicModelEntry, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PublicModelEntry$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PublicModelEntry' from JSON`,
  );
}
