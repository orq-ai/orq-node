/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  DatasetMetadata,
  DatasetMetadata$inboundSchema,
} from "./datasetmetadata.js";

/**
 * A dataset groups datapoints used for evaluation and optimization workflows.
 */
export type Dataset = {
  /**
   * Human-readable dataset name.
   */
  displayName: string;
  /**
   * Project that owns the dataset.
   */
  projectId: string;
  /**
   * Dataset counters maintained by the API.
   */
  metadata: DatasetMetadata;
  /**
   * Account that created the dataset.
   */
  createdById: string;
  /**
   * Account that last updated the dataset.
   */
  updatedById: string;
  /**
   * RFC3339 timestamp when the dataset was created.
   */
  created: string;
  /**
   * RFC3339 timestamp when the dataset was last updated.
   */
  updated: string;
  /**
   * Workspace that owns the dataset, derived from the authentication context.
   */
  workspaceId: string;
  /**
   * Unique dataset identifier.
   */
  id: string;
};

/** @internal */
export const Dataset$inboundSchema: z.ZodType<Dataset, z.ZodTypeDef, unknown> =
  z.object({
    display_name: z.string(),
    project_id: z.string(),
    metadata: DatasetMetadata$inboundSchema,
    created_by_id: z.string(),
    updated_by_id: z.string(),
    created: z.string(),
    updated: z.string(),
    workspace_id: z.string(),
    _id: z.string(),
  }).transform((v) => {
    return remap$(v, {
      "display_name": "displayName",
      "project_id": "projectId",
      "created_by_id": "createdById",
      "updated_by_id": "updatedById",
      "workspace_id": "workspaceId",
      "_id": "id",
    });
  });

export function datasetFromJSON(
  jsonString: string,
): SafeParseResult<Dataset, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Dataset$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Dataset' from JSON`,
  );
}
